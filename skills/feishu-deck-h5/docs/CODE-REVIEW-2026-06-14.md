# feishu-deck-h5 — Comprehensive Code Review

_Generated 2026-06-14 · multi-agent review (68 agents, 13 subsystems) with adversarial verification of every high-severity finding._

**86 verified findings** (of 87 raw; 1 refuted) — 0 critical · 6 high · 31 medium · 49 low.


## HIGH (6)

### `mutation-1` — import-html-slide.py mutates deck.json with no schema validation and no rollback — failed import leaves an invalid deck.json on disk
- **Severity:** HIGH  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/import-html-slide.py:656-666, 859-867`
- **Problem:** insert_into_json() is a second writer to deck.json that does NOT adopt deck-cli's write_deck_with_validation contract. It takes a .bak, splices new slides, writes deck.json, and returns. The ONLY validation is per-fragment HTML validation (validate_slide_fragment via validate.py) done BEFORE insertion — the resulting deck.json is never validated against deck-schema before being written. The post-write re_render (render-deck.py) does validate the schema and returns rc 2 on failure, but by then deck.json is already mutated on disk and is NEVER restored from the .bak. The caller in main() (lines 859-867) explicitly acknowledges this ('import mutated deck.json but the re-render FAILED — the deck may now be invalid') and just returns the error code, leaving a broken SSOT that the user must manually recover from the timestamped .bak. This directly violates the architecture invariant that deck.json must never be left corrupted without rollback.
- **Evidence:**
  ```
  deck["slides"][position:position] = new_slides
      deck_path.write_text(
          json.dumps(deck, ensure_ascii=False, indent=2), encoding="utf-8"
      )
  ...
          rc = re_render(target)
          if rc != 0:
              _err("import mutated deck.json but the re-render FAILED — the deck "
                   "may now be invalid. Fix the error shown above and re-render; "
                   "NOT reporting success.")
              return rc
  ```
- **Fix:** Route insert_into_json through (or mirror) deck-cli's write_deck_with_validation: write to a temp/in-memory copy, run validate-deck.py --strict against the candidate deck.json, and only os.replace it into place if it passes; on schema failure (or on re_render failure) restore from the .bak automatically. At minimum, on re_render failure auto-restore deck_path from the .bak created at line 658-659 before returning. Also import and use deck-cli.atomic_write_text instead of the plain non-atomic write_text at line 663.
- **Verifier:** Every load-bearing assertion is verified against the actual code.

(1) insert_into_json (deck-json/import-html-slide.py:584-666) is a second writer to deck.json that does NOT use deck-cli's validated-write contract. The file's imports (lines 41-51) contain no `deck_cli` / `write_deck_with_validation` / `atomic_write_text`, and grep for `validate-deck|deck-schema|write_deck_with_validation|atomic_write|os.replace|rollback|restore` inside import-html-slide.py returns nothing. It takes a .bak (lines 657-659: shutil.copy(deck_path, bak)), splices `deck["slides"][position:position] = new_slides` (line 662), and writes with a plain, non-atomic `deck_path.write_text(json.dumps(...))` (lines 663-665). There is no schema validation of the resulting deck and no rollback.

(2) The only validation is per-fragment HTML validation. validate_slide_fragment (lines 402-441) builds a temp full-deck HTML containing just the slide and runs assets/validate.py on it — purely HTML/rule-level, never the deck-schema. It runs in main() at line 806, BEFORE insertion (insert_into_json is called at line 860).

(3) re_render (lines 669-681) shells into render-deck.py, which DOES validate deck.json against the schema as its FIRST step (render-deck.py:2580-2590: runs validate-deck.py --strict; on failure `return 2`). But this happens AFTER deck.json has already been mutated on disk. render-deck.py's only rollback is _rollback_index_html (lines 2953-2962), which restores index.html via os.replace — it never restores deck.json (args.deck is read-only input). The deck.json schema check at 2585-2590 returns before the index.html backup is even created (line 2947), so nothing restores the SSOT.

(4) The caller (lines 859-867) on re_render rc!=0 prints exactly the quoted warning ("import mutated deck.json but the re-render FAILED — the deck may now be invalid …") and `return rc`, with no auto-restore from the .bak created at 658-659.

Reachability confirmed: insert_into_json copies `accent`/`decor`/`screen_label` straight from imported HTML data-* attrs (lines 641-646) and sets `_orig_layout` from `attrs.get("layout")` (line 634). The slide schema ($defs/slide, additionalProperties:false) constrains accent to accent_enum=["blue","teal","violet","purple","orange"] and decor to decor_token enum. A foreign/lifted imported slide carrying e.g. data-accent="cyan"/"green"/"red" or a non-canonical data-decor token yields a spliced deck.json that fails strict schema validation in render-deck — i.e. the corruption-on-disk path is reachable from realistic input, not just theoretical.

Severity: high is honest, not inflated. It violates the invariant "deck.json must never be left corrupted without rollback," and additionally uses a non-atomic write (torn file if killed mid-write). It is kept below critical because a timestamped .bak-pre-import-<ts> is created immediately before the write (lines 657-659), so the prior good state is manually recoverable — degradation requiring manual recovery rather than unrecoverable silent data loss. The suggested fix is sound: route the spliced deck through validate-deck.py --strict on a candidate before os.replace, and on schema/re-render failure auto-restore deck_path from the .bak; use an atomic write instead of write_text.

### `delivery-1` — Remote deliverable zip ships the ENTIRE 30 MB shared pool (all customers' logos) due to symlink + cp -R + zip dereference
- **Severity:** HIGH  ·  **Category:** security  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/package-deliverable.sh:96-99 (cp -R assets) / finalize.sh:117-128 (remote uses default --shared=link)`
- **Problem:** In 'remote' mode finalize.sh runs copy-assets WITHOUT --shared=copy, so it defaults to --shared=link, which makes output/assets/shared a symlink pointing at the canonical 30 MB skill pool (259 clientlogos, avatar packs, product kits, plus a stray .DS_Store). package-deliverable.sh then `cp -R "$OUT_DIR/assets" "$STAGE/assets"` which PRESERVES the inner symlink (macOS cp -R does not follow symlinks found during recursion), and finally `zip -q -X -r` DEREFERENCES the symlink and archives the whole pool. I reproduced end-to-end: a deck referencing exactly ONE shared logo produced a 26 MB lark-<cust>.zip containing all 259 clientlogo PNGs (377 files total) and assets/shared/.DS_Store. This is both a confidentiality leak (every other customer's brand logo ships to this customer) and gross bloat that defeats the entire stated purpose of --shared=link ('Saves ~5-30 MB per run'). Library mode is safe because it uses --shared=copy (referenced files only).
- **Evidence:**
  ```
  finalize.sh:117  COPY_ARGS=("$OUT_DIR")
  ... (only library mode appends --shared=copy)
  package-deliverable.sh:96  if [ -d "$OUT_DIR/assets" ]; then
    97    cp -R "$OUT_DIR/assets" "$STAGE/assets"
    ...
    119  ( cd "$STAGE" && zip -q -X -r "$ZIP_PATH" "${ZIP_ITEMS[@]}" )
  # reproduced: unzip -l lark-test-cust-2026-01-01.zip | grep clientlogo/ | wc -l => 259; zip size 26 MB; assets/shared/.DS_Store present
  ```
- **Fix:** Make the remote path self-contain only referenced shared files: in finalize.sh remote mode pass --shared=copy to copy-assets (same as library mode) before packaging; OR in package-deliverable.sh, if output/assets/shared is a symlink, re-run copy-assets --shared=copy on OUT_DIR first, or use `zip --symlinks` to store the link verbatim (not portable) — copy mode is the right fix. Also add --exclude '.DS_Store' / strip dotfiles from the staged assets so system metadata never ships.
- **Verifier:** Reproduced end-to-end and every mechanistic claim holds.

1) Default shared mode is link: copy-assets.py line 174 `shared_mode = "link"`; docstring (lines 21-27) confirms link is the default and creates a symlink at output/assets/shared pointing at the canonical pool, "Saves ~5-30 MB per run." For a shared/* ref in link mode, lines 249-254 call ensure_shared_symlink (lines 150-170), which does `target.symlink_to(canonical)` to skill_root/assets/shared.

2) Remote mode does NOT harden to copy: assets/finalize.sh lines 117-120 build COPY_ARGS=("$OUT_DIR") and append "--shared=copy" ONLY when MODE==library; remote (lines 193-201) just runs package-deliverable.sh on the link-mode output. (The claim cited the file as finalize.sh:117; it is actually assets/finalize.sh:117 — same line numbers, only the assets/ prefix was omitted.)

3) package-deliverable.sh lines 96-99 do `cp -R "$OUT_DIR/assets" "$STAGE/assets"` and line 119 `( cd "$STAGE" && zip -q -X -r "$ZIP_PATH" ... )`. I verified in isolation that macOS `cp -R` PRESERVES an inner symlink encountered during recursion (staged assets/shared remained a symlink) and that `zip -X -r` DEREFERENCES it, embedding the whole target tree. There is no `--exclude`, no `--symlinks`, and no .DS_Store stripping anywhere in package-deliverable.sh.

4) Full pipeline reproduction: a deck referencing exactly ONE shared logo (clientlogo/Babycare.png), run through copy-assets (default link) + package-deliverable.sh, produced lark-test-cust-2026-01-01.zip = 25.8 MB, 369 entries, 259 clientlogo PNGs (258 belonging to OTHER customers), plus assets/shared/.DS_Store. Confirmed the pool itself is 30M / 358 files / 259 clientlogos / has a .DS_Store (the claim's 30 MB / 259 / .DS_Store figures all match). (Note: a naive `grep clientlogo` on `unzip -l` output returns 0 because BSD grep treats the CJK filenames as binary — I counted with Python/zipfile to avoid that false-negative; the entries are genuinely present.)

5) The claim's "library mode is safe" assertion also holds: package-ingest.sh uses --shared=copy (finalize.sh line 119) and additionally rejects symlinks (lines 87-88) and strips/rejects .DS_Store/__MACOSX/._* (lines 89-90, 164, 174-175), using copytree(symlinks=False). This asymmetry proves the intended invariant (ship only referenced files, self-contained, no system metadata) and that the remote path regresses it.

Harm: (a) confidentiality — 258 other customers' brand logos ship inside a customer-facing deliverable zip; (b) the leak defeats the documented purpose of --shared=link and balloons a ~KB deck to ~26 MB; (c) assets/shared/.DS_Store also leaks. Category is fairly security (third-party data disclosure via deliverable) layered on a correctness/intent regression. Claimed severity high is accurate, not inflated; I keep high. Suggested fixes are sound: in finalize.sh remote mode pass --shared=copy (mirrors library) so only referenced shared files are real-copied before zipping, and add a .DS_Store/dotfile exclude in package-deliverable.sh (zip --exclude '*.DS_Store' or strip from STAGE).

### `prose-1` — delivery.md routes the deck-inline delivery step to `build.sh --inline`, which rebuilds the bundled SAMPLE deck, not the user's run deck
- **Severity:** HIGH  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `references/delivery.md:76, 329`
- **Problem:** Mode A (the documented DEFAULT for "show me / 给客户看 / IM 转发") and Mode 3 both instruct the agent to produce the inline single-file artifact via `bash build.sh --inline` and ship `examples/sample-deck-inline.html`. But build.sh is hardcoded to read the skill's `$ROOT/_body.partial.html` and emit `$ROOT/examples/sample-deck.html` / `$ROOT/examples/sample-deck-inline.html` — it rebuilds the skill's bundled DEMO deck and has no parameter for a run's deck.json/index.html. The actual per-run inline path is `assets/inline-assets.py` (or `render-deck.py --inline` / `deck-cli render --inline`). An agent following the literal instruction would inline and hand back the sample deck instead of the customer's deck. The parenthetical "(or its renamed copy under runs/<ts>/output/)" hints a per-run copy is wanted but never names the correct tool.
- **Evidence:**
  ```
  delivery.md L76: "`bash build.sh --inline` → ship `examples/sample-deck-inline.html` (or its renamed copy under `runs/<ts>/output/`)"; delivery.md L329: "Use `build.sh --inline` to produce a fully self-contained single file." build.sh L35-36: `OUT_LINK="$ROOT/examples/sample-deck.html"` / `OUT_INLINE="$ROOT/examples/sample-deck-inline.html"` — fixed sample paths, body from `$ROOT/_body.partial.html`.
  ```
- **Fix:** Replace `build.sh --inline` in delivery.md Mode A/Mode 3 with the per-run inline tool, e.g. `python3 assets/inline-assets.py runs/<ts>/output/index.html runs/<ts>/output/lark-<customer>-<date>.html` (or `render-deck.py --inline`). Keep `build.sh --inline` only where it is correctly described as building the SKILL's sample/demo deck (validator-rules.md R-DOC-INTEGRITY note is fine).
- **Verifier:** Every element of the claim is independently confirmed by reading the actual files.

(1) delivery.md L76 is Mode A, explicitly marked the DEFAULT ("default for 'show me / 给客户看 / IM 转发 / 链接预览'") and instructs: "`bash build.sh --inline` → ship `examples/sample-deck-inline.html` (or its renamed copy under runs/<ts>/output/)". delivery.md L328-329 (Mode 3, view-only) repeats: "Use `build.sh --inline` to produce a fully self-contained single file."

(2) build.sh is hardcoded to the skill's bundled DEMO deck: L35-36 set OUT_LINK="$ROOT/examples/sample-deck.html" and OUT_INLINE="$ROOT/examples/sample-deck-inline.html"; L54 and L123 build the body via `cat "$ROOT/_body.partial.html"`. The arg loop (L16-23) accepts only --inline/--validate/--skip-preflight — there is NO parameter for a run's deck.json or index.html. _body.partial.html is a static fixture (its own header: "Contains every layout once (13 slides, all canonical recipes)") and grep shows it is only ever cat'd by build.sh, never regenerated from a run. So `build.sh --inline` produces the skill's demo deck regardless of any run.

(3) The correct per-run inline tools exist and are exactly as the claim states: assets/inline-assets.py takes a positional `html` input path + `--out` (parse_args L248-253, main L256-263), and render-deck.py has a real --inline flag (L2456) plumbed through deck-cli render --inline (deck-cli.py L1193, L1289).

(4) delivery.md never names any of the correct per-run tools: grep for `render-deck|inline-assets|deck-cli render` in delivery.md returns zero matches. So an agent following the literal default route would inline and hand back examples/sample-deck-inline.html (the demo), not the customer's deck — a silent wrong-deck delivery on the DEFAULT path.

Additional corroboration that this is a systemic convention bug, not a typo: finalize.sh L46-49 explicitly redirects users wanting inline delivery to "run from the skill root: bash build.sh --inline", and converting-existing-material.md L974 + reskin.md L58 + prototype-embed.md L345 all reference `build.sh --inline` as the inline path. The "(or its renamed copy under runs/<ts>/output/)" parenthetical hints a per-run artifact is wanted but never names the tool that would create it.

Severity high (not critical): this is a docs/routing-correctness defect, not a code-corruption or data-loss bug, and a deck.json remains intact. But it sits on the DEFAULT delivery path, the failure is silent (a syntactically valid, openable HTML — just the wrong content), and the consequence is shipping a demo deck to a customer. The suggested fix (replace `build.sh --inline` in Mode A/Mode 3 with `render-deck.py --inline` or `assets/inline-assets.py runs/<ts>/output/index.html --out ...`, and reserve build.sh for building the skill's sample/demo) is correct and should also be propagated to finalize.sh L46-49 and converting-existing-material.md to fix the systemic drift.

### `subskill-1` — Publisher skips the validator gate entirely when no deck.json sits in the output dir
- **Severity:** HIGH  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `subskills/publisher/publish.py:528-535`
- **Problem:** The mandatory deck-validator gate is only enforced when `output_dir/deck.json` exists. When the publisher is invoked with `--html <external.html>` and no `--task-id`, main() creates a fresh empty output_dir (lines 531-532) that contains no deck.json, so the condition at line 534 is False and the gate is a no-op. The publisher then proceeds to publish the arbitrary HTML to the EXTERNAL Magic Page service (https://magic.solutionsuite.cn) with zero validation. The SKILL.md states a deck-validator pass verdict is a hard precondition for publishing.
- **Evidence:**
  ```
  if not args.allow_unaudited and output_dir.exists() and (output_dir / "deck.json").exists() and not audit_passed(output_dir):
          raise SystemExit("publisher: deck-validator pass verdict is required before publishing")
  ```
- **Fix:** Make the gate fail-closed: when not --allow-unaudited, require positive PASS evidence for the artifact being published regardless of deck.json presence. Either run check-only.py --gate publish against the resolved html_path (see fix for subskill-2) and require it to pass, or raise SystemExit when no audit verdict can be established. Do not let absence of deck.json silently disable the gate.
- **Verifier:** Verified against /Users/bytedance/.claude/skills/feishu-deck-h5/subskills/publisher/publish.py. The cited evidence at line 534 matches exactly: `if not args.allow_unaudited and output_dir.exists() and (output_dir / "deck.json").exists() and not audit_passed(output_dir): raise SystemExit(...)`. In main() (lines 520-535), when invoked with `--html <external>` and no `--task-id`, lines 531-532 create a fresh empty output_dir (`output_dir = RUNS / task_id / "output"; output_dir.mkdir(parents=True, exist_ok=True)`) that contains no deck.json. I reproduced the boolean: with allow_unaudited=False and a freshly-created empty output_dir, the condition evaluates to False (deck.json absent short-circuits), so audit_passed() is never called and no SystemExit is raised — the gate is a no-op. Control then flows to resolve_html (line 537, which accepts the external --html as-is) and publish_magic_page (line 543). I read publish_magic_page (lines 272-385) end to end: it has NO validator gate — it only checks for data:image payloads and unhosted dependencies, then publishes to DEFAULT_MAGIC_BASE_URL = https://magic.solutionsuite.cn (line 33). A grep for validate/check-only/audits/verdict across publish.py shows the validator is never actually run; audit_passed() (lines 105-121) merely READS a pre-existing audit-report.json / AUDIT_REPORT.md, so even on the --task-id path the gate is evidence-reading not real validation. The documented contract makes this a genuine defect: subskills/publisher/SKILL.md line 39 ("必须有 deck-validator 通过结论。只有本地调试/夹具可用 --allow-unaudited"), line 40 (validation must run on the exact bytes about to be published), and hard rule line 140 ("不绕过 validator 发布失败 HTML"); controller SKILL.md line 72-74 also states validation is required before publisher confirmation. The --html no-task-id invocation is an explicitly documented first-class command (publisher SKILL.md lines 62-68), so the bypass sits squarely in a sanctioned happy path, not an obscure corner. Severity high (not critical): it does not corrupt the deck.json SSOT and is not an injection/traversal vector, and real publishing still needs a MAGIC_TOKEN plus human confirmation; but it fully defeats a documented hard gate and can push unvalidated HTML to an external production service. The suggested fix is sound in spirit (fail-closed: require positive PASS evidence regardless of deck.json presence), with the caveat that audit_passed currently only reads a report rather than validating the actual bytes — a complete fix should validate the resolved html_path itself.

### `subskill-2` — Publisher reuses a stale render-time audit verdict and never validates the actual publish bytes (R-BAKED-DOM contract drift)
- **Severity:** HIGH  ·  **Category:** contract-drift  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `subskills/publisher/publish.py:105-121,534,272-385`
- **Problem:** publisher/SKILL.md explicitly mandates: validation must run on the exact HTML bytes about to be published, must NOT reuse an old render-time PASS, and must catch R-BAKED-DOM (data-idx=/baked deck-ui/.deck[data-js-ready] indicating a saved live DOM). The code does none of this: the only gate is audit_passed(output_dir), which reads a pre-existing audit-report.json/AUDIT_REPORT.md (a stale render-time verdict), and there is no invocation of any validator anywhere in publish.py (grep for validate/check-only returns NONE). Worse, the bytes actually published are working_html (post inline-assets + magic-page-assets rewriting, lines 305-337), which differ from the bytes any prior audit ran on. The documented contract and the implementation are out of sync.
- **Evidence:**
  ```
  def audit_passed(output_dir: Path) -> bool:
      report = output_dir / "audit-report.json"
      ...
          if verdict == "pass" or status == "pass":
              return True   # (reuses stale render-time verdict; no re-check of publish bytes)
  # and no validator/check-only call exists in publish.py at all
  ```
- **Fix:** Before publishing, run check-only.py (or the deck validator) against the FINAL working_html bytes (after inline/magic-page asset prep) with the publish/R-BAKED-DOM gate, and fail-closed on a non-pass. Stop treating a pre-existing output_dir audit-report as sufficient. Align code with publisher/SKILL.md or, if reuse is intended, change SKILL.md — but the safer fix is to validate the publish bytes.
- **Verifier:** Verified every material point against the actual code.

1) Contract (publisher/SKILL.md lines 40-44) explicitly mandates: "校验必须跑在即将发布的那一份 HTML 字节上,不能复用旧的'渲染时已过'结论。重点拦 R-BAKED-DOM:若发布物含 data-idx= / class=\"deck-ui\" / .deck 带 data-js-ready ... 必须从 deck.json 重渲出干净版再发". So byte-level validation on the publish-bound bytes, no reuse of the render-time verdict, and R-BAKED-DOM detection are all documented requirements.

2) publish.py does the opposite. audit_passed() (lines 105-121) only READS a pre-existing output_dir/audit-report.json or AUDIT_REPORT.md and returns True on verdict/status == "pass" — i.e. it reuses the old render-time verdict, the exact thing the contract forbids. The sole gate (line 534) requires that stale report (or SystemExits). I grepped publish.py: the only subprocesses spawned are inline-assets.py, magic-page-assets.py, and node magic-page-publish.js — there is NO invocation of check-only.py / run-audits.py anywhere. So no validator runs on the publish bytes.

3) The published bytes differ from the audited bytes: working_html starts as html_path, becomes prepared (inline-assets) then packaged (magic-page-assets with TOS-rewritten URLs), working_html = packaged (line 337), and the node publisher publishes working_html (line 366). Any render-time audit ran on the original index.html, not this rewritten artifact.

4) R-BAKED-DOM is a genuinely registered byte-path validator rule (assets/business-rules.yaml:662; references/validator-rules.md:23 explicitly "Runs on BOTH --visual and --no-visual (byte/source path)"; check-only.py:75 FAMILIES; implemented in run-audits.py audit_baked_runtime_dom_bytes at lines 815/974). So running check-only.py on the publish bytes WOULD catch it; publish.py never does.

5) The sibling subskill importer/ingest.py proves the intended/correct pattern: it has the IDENTICAL audit_passed() PLUS ensure_quality_gate() (lines 113-149) that, when no PASS evidence exists, actually runs CHECK_ONLY = assets/check-only.py as a subprocess against the html_path. publish.py has no CHECK_ONLY constant and no equivalent — a clear asymmetry/drift within the same subskill family.

6) No compensating control: F-285 self_check.py covers only 404/broken-link, font-fallback, and per-page visual diff (verified by grep — zero baked-DOM/data-idx/R-BAKED detection; it even waits FOR .deck[data-js-ready] as a normal init signal), and it is skipped on no-login/no-app_url and downgradable with --self-check-soft.

Net: documented contract and implementation are out of sync, and the missing fresh byte-level validation means a baked/edited-after-audit deck can be published unvalidated (R-BAKED-DOM → page count frozen at 1, duplicate UI). Category contract-drift is accurate (with a correctness/robustness dimension). Calibration: the gate isn't fully absent (missing report SystemExits) and exploitation needs a stale-edit or baked-DOM input, but both are realistic and the contract is genuinely contradicted — high is honest, not inflated. Suggested fix (run check-only.py on the final working_html with the R-BAKED-DOM/publish gate, fail-closed) is correct.

### `subskill-3` — Parser HTML-asset materializer follows ../ refs out of source dir and copies arbitrary readable files into the deliverable
- **Severity:** HIGH  ·  **Category:** security  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `subskills/parser/parse.py:606-661`
- **Problem:** local_html_asset_path() resolves `html_path.parent / clean` and returns it if it is a file, with NO containment check — `..` segments are not blocked (only URLs/data/#/mailto/tel are). A source HTML (explicitly marked untrusted per F-287) containing e.g. <img src="../../../../../../Users/<user>/.ssh/id_rsa"> resolves to a real file outside the source tree, and materialize_html_assets() then shutil.copy2() copies it into out_dir/assets/html/<bucket>/ and records the runtime path in the dossier's material_layer. The `..`/absolute check at lines 654-655 only sanitizes the DESTINATION filename (flattening to origin.name); it does not prevent reading/copying the out-of-tree origin. Downstream renderers/inliners may then embed that file content into the published deck — an arbitrary-file-read-and-exfiltrate from untrusted input.
- **Evidence:**
  ```
  candidate = (html_path.parent / clean).resolve()
      return candidate if candidate.is_file() else None
  ...
                  if rel_path.is_absolute() or any(part == ".." for part in rel_path.parts):
                      rel_path = Path(origin.name)   # sanitizes DEST only; origin already read
                  ...
                      shutil.copy2(origin, dest)
  ```
- **Fix:** In local_html_asset_path, after resolving `candidate`, reject it unless it is contained within the source HTML's directory tree (e.g. check candidate.resolve().is_relative_to(html_path.parent.resolve())) — return None for any ref that escapes the source dir, and add a warning. Apply the same containment check in resolve_markdown_media (line 717-721), which has the identical pattern.
- **Verifier:** Confirmed by reading the code and an empirical repro. At subskills/parser/parse.py:606-615, local_html_asset_path() does `candidate = (html_path.parent / clean).resolve()` and returns it if `candidate.is_file()`, with NO containment check — is_url() (line 117-118) only matches `https?://`, so bare `../` refs (and file://) pass through. In materialize_html_assets() (618-661), origin = local_html_asset_path(source_path, ref) and shutil.copy2(origin, dest) at line 659 copies it. The check at 654-655 (`rel_path.is_absolute() or any(part == "..")`) only flattens the DESTINATION filename to origin.name; the out-of-tree origin is still read and copied. The runtime path is then recorded in html_assets_materialized (660-661) and flows into material_layer (lines 1397-1408), which the dossier handoff (line 1683) tells downstream designer/renderer to consume as local assets. Sources are explicitly marked untrusted: true per F-287 (lines 108-114, 1655), so untrusted-input-drives-file-read is exactly the codebase's own threat model. I reproduced it end-to-end: a source HTML containing <img src="../secret/id_rsa"> resolved to /private/tmp/.../secret/id_rsa (outside the source dir) and was copied into out/assets/html/page/id_rsa with the secret bytes intact, and recorded in html_assets_materialized. The secondary claim is also confirmed: resolve_markdown_media (line 717-721) has the identical missing-containment pattern; it does not shutil.copy2 but it does read the out-of-tree file via image_dimensions() and records repo_rel(local) (line 808-815). Severity held at high (not critical): the operator running the parser typically also supplies the source, so this is a malicious-source-HTML-handed-to-victim scenario, not a remote attack; and the final exfil hop (renderer actually embedding/publishing the copied file content) is plausible but I did not independently verify a renderer inlines arbitrary non-image bytes — the file is unconditionally copied into the run deliverable tree, which is itself the real arbitrary-file-read-into-artifact boundary violation. The suggested fix is correct: after resolve, reject candidates not contained in html_path.parent.resolve() (e.g. is_relative_to) in both local_html_asset_path and resolve_markdown_media, returning None + warning on escape.


## MEDIUM (31)

### `mutation-2` — Path traversal in cmd_paste asset copy — a crafted source deck.json can write files outside the destination deck dir
- **Severity:** MEDIUM  ·  **Category:** security  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/deck-cli.py:868-907`
- **Problem:** _copy_slide_assets scans the pasted slide's text for input/, prototypes/, and assets/shared/ references with regexes whose capture groups only exclude whitespace/quotes/brackets/?#\ — they DO allow '../'. The captured fname is then used directly as `d = dst_dir / 'input' / fname` (and equivalents) and shutil.copy2(s, d). With a reference like `input/../../etc/evil.png` pointing at an existing source file, the destination resolves OUTSIDE the destination deck directory (confirmed: dst_dir/input/../../etc/evil.png -> /etc/evil.png). Since paste's documented use case is copying a slide from ANOTHER deck.json (the lift architecture), a malicious or careless foreign deck can cause arbitrary file writes. Notably the LATER bare-media loop (lines 921-924) explicitly guards against '../' but the structured input/ prototypes/ assets/shared/ loops above it do not — inconsistent.
- **Evidence:**
  ```
  for fname in sorted(set(re.findall(r"input/([^\s\"'<>()\\?#]+)", text))):
          s = src_dir / "input" / fname
          d = dst_dir / "input" / fname
          if s.is_file():
              d.parent.mkdir(parents=True, exist_ok=True)
              if not d.exists() or s.stat().st_mtime > d.stat().st_mtime:
                  shutil.copy2(s, d)
  ```
- **Fix:** Before each copy, containment-check the resolved destination (and ideally source): `if not str(d.resolve()).startswith(str(dst_dir.resolve()) + os.sep): skip+flag`, exactly the relative_to(deck_root) guard that import-html-slide already applies in its inner recursion. Apply to all three structured loops (input/, prototypes/, assets/shared/). Optionally also reject fname containing '..' before building the path.
- **Verifier:** CONFIRMED the core defect, with a severity correction from high→medium based on a constraint I verified.

Code (deck-cli.py:868-907, read directly): the three structured asset-copy loops in _copy_slide_assets capture fname/seg/ref with regexes `re.findall(r"input/([^\s\"'<>()\\?#]+)", text)` (and prototypes/ , assets/shared/ equivalents). I confirmed by running the regex that `input/../../etc/evil.png` is captured as `../../etc/evil.png` — the char class excludes whitespace/quotes/brackets/?#\ but NOT `..`. The captured value is used unsanitized: `d = dst_dir / "input" / fname; ... shutil.copy2(s, d)`. A tiny end-to-end sim showed `dst_dir/input/../secret.png` resolves to `dst_dir/secret.png` (outside input/), and `../../../shared_payload.png` resolves to a path OUTSIDE the deck dir entirely — so the write CAN escape dst_dir, not just dst_dir/input.

Inconsistency is real: the bare-media loop at 921-924 explicitly guards (`ref.startswith("../") or "/../" in ref: continue`) but the three structured loops above it do not. The suggested-fix precedent is also real: sibling tool import-html-slide.py:373 applies `d2.resolve().relative_to(deck_root)` with a 'never write outside deck' comment in its inner asset recursion — exactly the guard missing here.

Reachability: cmd_paste (lines 999-1060) sets src_dir = parent of --from deck.json and scans the pasted slide's text (_slide_asset_text) for asset refs. The documented lift use case is pasting a slide from ANOTHER (foreign) deck.json, so the asset-ref text is attacker/foreign-authored. No upstream sanitization of fname exists. So the bug is reachable.

Severity correction (high→medium): the SAME fname (same ../ depth) is used for BOTH s (src_dir/input/fname) and d (dst_dir/input/fname), and the copy only fires if s.is_file(). I verified this symmetry. Consequences: (1) bytes always come from a file that exists relative to the source bundle the attacker ships, so it is not an arbitrary-CONTENT-to-arbitrary-absolute-path primitive — the claim's '→ /etc/evil.png' only works if a mirror source file exists, because root-clamping ../ affects the source identically; (2) the attacker-guaranteed escape (tail shippable inside their bundle, k=1) lands one level up = inside the victim DECK dir but outside input/ (e.g., clobber the victim deck.json/config). Escaping the deck dir entirely (k>=2) is confirmed reachable but requires a pre-existing source file above the src deck dir on the operator's machine — opportunistic, not arbitrary. Overwrite is further gated by `not d.exists() or s newer`. These constraints make it a genuine but bounded path traversal, not the unconstrained arbitrary-file-write that 'high' implies. The fix (resolve()+relative_to(dst_dir) containment check, or reject fname containing '..') is correct, low-risk, and matches the existing precedent; it should be applied to all three structured loops.

### `mutation-3` — Path traversal in import copy_and_rewrite_assets top-level loop — guard present in recursion but missing in primary loop
- **Severity:** MEDIUM  ·  **Category:** security  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/import-html-slide.py:350-360`
- **Problem:** copy_and_rewrite_assets resolves each scanned ref against the source dir and copies it to `deck_dir / f"{ns}/{ref}"`. If ref contains '../' the destination escapes the deck dir (confirmed: deck_dir/assets/imported/<slug>/../../../../tmp/evil.png -> /tmp/evil.png). The author clearly knew this guard was needed — the ONE-LEVEL inner recursion at lines 372-375 does `d2.resolve().relative_to(deck_root)` and skips on ValueError — but the top-level loop has no such check, so the primary (and most common) copy path is unprotected when importing a crafted/foreign HTML fragment.
- **Evidence:**
  ```
  for ref in _scan_local_refs(frag):
          src_path = (src_dir / _strip_dotslash(ref)).resolve()
          if not src_path.is_file():
              missing.append(ref)
              continue
          dest_rel = f"{ns}/{_strip_dotslash(ref)}"
          dest_path = deck_dir / dest_rel
          dest_path.parent.mkdir(parents=True, exist_ok=True)
          shutil.copy2(src_path, dest_path)
  ```
- **Fix:** Mirror the inner-recursion guard in the top-level loop: after computing dest_path, do `try: dest_path.resolve().relative_to(deck_dir.resolve()); except ValueError: missing.append(ref); continue` (and skip the copy + the mapping entry) so a ../-bearing ref can never write outside the target deck.
- **Verifier:** The cited code matches the file exactly. In copy_and_rewrite_assets (deck-json/import-html-slide.py:350-360), the top-level loop computes dest_path = deck_dir / f"{ns}/{_strip_dotslash(ref)}" and shutil.copy2's into it with NO containment check. The helpers do not neutralize traversal: _is_local_asset_ref (lines 285-293) only rejects data:/http:/#/etc., NOT '../'; _strip_dotslash (296-299) strips only leading './', leaving '../' intact. The asymmetry the reviewer claims is real and confirmed: the one-level inner recursion at lines 371-375 does d2.resolve().relative_to(deck_root) with `except ValueError: continue`, but the primary loop omits this guard entirely. Reachability confirmed: copy_and_rewrite_assets is called by default in main (line 827-831, gated only by --no-copy-assets), with src_dir = src.parent (the imported HTML file's dir) and frag = the scanned source HTML — so an imported/foreign HTML fragment with e.g. <img src="../../../../payload.png"> drives the destination. I reproduced the full write end-to-end: with a deep deck dir (the realistic case, where ns="assets/imported/<slug>" adds 3 path components of depth), a ref of "../../../../payload.png" whose source file exists resolved dest to /tmp/travX/a/b/c/d/e/payload.png — OUTSIDE the deck root /tmp/.../deckroot/ — and shutil.copy2 actually wrote the file there; relative_to(deck_root) raised ValueError (i.e. the inner guard WOULD have blocked it, but the top loop doesn't run it). Primitive nature: because the same `ref` string is used for both src_path and dest_path, source-must-exist (is_file) couples the two, so it isn't an unconstrained write-anywhere; it is a write/overwrite into directories above/beside the deck root using content of an existing referenced file. That is a genuine path-traversal escape of the intended assets/imported sandbox, but the threat model is a local interactive importer where the user voluntarily imports HTML they selected, and the src-existence coupling limits arbitrary system-file clobbering. The reviewer's medium severity and suggested fix (mirror the relative_to(deck_dir) guard in the top-level loop, skipping the copy + mapping entry, and route to missing) are both correct and not inflated.

### `mutation-5` — import-html-slide.py deck.json write is non-atomic (no F-269 protection)
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `deck-json/import-html-slide.py:663-665`
- **Problem:** deck-cli.py introduced atomic_write_text (F-269) precisely because Path.write_text truncates-then-streams: a kill/disk-full/exception mid-write leaves a half-written (torn) deck.json that looks valid to the next reader. render-deck.py adopted it too. import-html-slide.py — the other deck.json mutator — still uses a plain non-atomic write_text, so an interrupted import can torn the SSOT. Combined with mutation-1 (no rollback) this is the weakest write path for the most critical file.
- **Evidence:**
  ```
  deck["slides"][position:position] = new_slides
      deck_path.write_text(
          json.dumps(deck, ensure_ascii=False, indent=2), encoding="utf-8"
      )
  ```
- **Fix:** Import atomic_write_text from deck-cli (render-deck already does this via _load_atomic_write_text at lines 106-124) and use it for both the deck.json write here and the Mode-B html write at line 729.

### `delivery-2` — inline-assets.py <img> inlining corrupts earlier attributes whose value equals the src filename
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/inline-assets.py:217, 222`
- **Problem:** inline_img_tags replaces the asset ref via `match.group(0).replace(src, uri)` / `.replace(src, attr_escape(...))`. str.replace replaces ALL occurrences of the src string within the matched tag fragment. The regex `<img\s+[^>]*src=[...]` greedily includes any attributes BEFORE src, so if an earlier attribute holds the same string (e.g. `<img data-name="logo.png" src="logo.png">`, or `alt="logo.png"` placed before src), that attribute is also rewritten to the data URI / staging path. I reproduced this: data-name="logo.png" became data-name="data:image/png;base64,...". The result is a malformed/oversized attribute and, for staging-ref mode, a wrong path. The same .replace-on-whole-tag pattern is used for both the base64 and the --no-image-inline staging branch.
- **Evidence:**
  ```
  inline-assets.py:217  return match.group(0).replace(src, attr_escape(upload_staging_ref(asset)))
  inline-assets.py:222  return match.group(0).replace(src, uri)
  # repro: <img data-name="logo.png" src="logo.png"> -> data-name="data:image/png;base64,iVBOR..." (data-name corrupted)
  ```
- **Fix:** Replace only the src attribute value, not the whole-tag substring. Either capture the src attribute span in the regex and rebuild the attribute (e.g. `f'src="{uri}"`), or do a targeted replace of `src="{src}"` / `src='{src}'` instead of the bare value. Mirror magic-page-assets.py's group-based RESOURCE_ATTR_RE approach which rewrites only group(5).
- **Verifier:** Read assets/inline-assets.py:206-224. The code matches the claim exactly: inline_img_tags's replace_img returns `match.group(0).replace(src, attr_escape(upload_staging_ref(asset)))` (line 217, staging branch) and `match.group(0).replace(src, uri)` (line 222, base64 branch). The driving regex (line 224) is `<img\s+[^>]*src=["']([^"']+)["']`, where the greedy `[^>]*` includes every attribute BEFORE src in group(0). str.replace then substitutes ALL occurrences of the src string within that matched fragment.

I reproduced the exact claimed repro with a Python snippet: `<img data-name="logo.png" src="logo.png">` -> `<img data-name="data:image/png;base64,..." src="data:image/png;base64,...">` — the earlier data-name attribute is corrupted. The bug is in fact slightly broader than claimed: any preceding attribute whose value merely CONTAINS the src string is also mangled, e.g. `<img alt="see assets/logo.png for details" src="assets/logo.png">` -> `alt="see DATAURI for details"`. (Trailing attributes after src are safe because the regex match ends at the src closing quote.) The identical whole-tag .replace anti-pattern also appears uncited at line 159 (rewrite_preload_image_links: `tag.replace(href, ...)`), corroborating the bug class.

I verified the suggested fix's reference is accurate: the sibling assets/magic-page-assets.py uses RESOURCE_ATTR_RE (line 54, non-greedy with captured groups) and replace_resource_attr (line 332) rebuilds the attribute from `prefix + quote + value + quote`, rewriting only the value group — immune to this corruption. inline-assets.py should mirror this (capture/rebuild the src attribute span, or replace `src="{src}"` targeted) rather than .replace on the bare value over the whole tag.

Reachability/severity calibration: inline-assets.py runs over the full delivered index.html as a delivery-packaging step (main() line 271), and raw/author HTML pages are a first-class deck feature, so author `<img>` tags with alt/title/data-* attributes referencing the filename are reachable. However the DOMINANT path — render-deck.py-generated img tags (render-deck.py:1922, 1932) — is SAFE, because those tags only precede src with `class="el"`, `data-el-id`, `loading="lazy"`, none of whose values contain the src path. So the defect produces a wrong/malformed delivery artifact (corrupted attribute; in staging mode a wrong path) only for author-authored img tags, and never corrupts deck.json (the SSOT is untouched by this tool). Real, reachable, data-integrity defect in the delivered HTML, but confined to a non-default page type — medium is a fair severity (not critical; deck.json integrity is not at risk).

### `delivery-3` — assets-manifest.yaml still lists referenced-but-missing assets, so the manifest is inconsistent with package contents (commit 6244ed7 only half-fixed this)
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/copy-assets.py:260, 414, 595-627`
- **Problem:** Commit 6244ed7's stated goal was that missing assets used to be '文件照样写进 assets-manifest.yaml' (still written to the manifest) yet not in the package. The commit added an end-of-run warning but did NOT stop those files from being listed in the manifest. `referenced.add(...)` is called BEFORE the origin-existence check in both replace_skill (line ~260) and replace_local_asset (line ~414); a missing file ends up in both `missing_sources` AND `referenced`, and the manifest's framework/deck-local lists are built from `referenced` with no subtraction of `missing_sources`. I reproduced: referencing assets/no-such-cover.jpg yields the new ❌ warning but assets-manifest.yaml still lists `- assets/no-such-cover.jpg` under framework:. A downstream consumer (slide-library ingest) trusting the manifest will look for a file that isn't in the package — the original defect the commit claimed to close.
- **Evidence:**
  ```
  copy-assets.py:260  referenced.add(str(target.relative_to(out_dir)))  # added before origin.exists() check at :261
  copy-assets.py:597  for rel in sorted(referenced):  # builds framework/deck-local; never subtracts missing_sources
  # repro: manifest framework: contains '- assets/no-such-cover.jpg' while ❌ warning fired for the same path
  ```
- **Fix:** Before building the manifest, exclude missing files: `for rel in sorted(referenced - missing_sources):`. (Keep shared_refs as-is since shared files are validated separately.) This makes the manifest reflect only files actually present in output/.
- **Verifier:** Independently confirmed every element. In copy-assets.py both replace_skill (referenced.add at line 260, before the origin.exists() check at 261; missing path added to missing_sources at 277) and replace_local_asset (referenced.add at 414, before existence check at 415; missing added to missing_sources at 427) put missing files into BOTH referenced and missing_sources. The manifest loop at line 597 `for rel in sorted(referenced):` builds framework/deck-local lists from the full referenced set with zero subtraction of missing_sources — grep over the whole file shows no `referenced - missing_sources` anywhere. Commit 6244ed7's own message admits the prior bug ("文件照样写进 assets-manifest.yaml"); `git show 6244ed7` confirms it added only the missing_sources tracking set and the end-of-run ❌/⚠️ summary, never a manifest subtraction — a genuine half-fix. Live reproduction: created /tmp/delivery3-repro with an index.html referencing assets/no-such-cover.jpg, ran the real script with --shared=skip; it fired `❌ 1 个被引用的资源找不到源文件` for assets/no-such-cover.jpg yet emitted assets-manifest.yaml listing `- assets/no-such-cover.jpg` under framework:, and `find assets -type f` confirms the file is NOT in the package. The manifest therefore lies about package contents. A real downstream consumer exists: subskills/importer/ingest.py (lines 269-320) reads assets-manifest.yaml (deck_h5_output_for_assets) to drive slide-library ingest-assets.py rewriting. The suggested fix (`for rel in sorted(referenced - missing_sources)`) is sound and safe: prune (lines 564-588) runs BEFORE the manifest loop so prune-protection via referenced is unaffected, and the shared list is built from shared_refs (not referenced) so it stays untouched. Severity medium not high: the script loudly warns the author at packaging time and the importer has secondary backstops (ingest-assets.py + the post-rewrite has_unrewritten_framework_refs check), so the inconsistent manifest is unlikely to silently corrupt a real delivery, but it remains a real data-integrity contract inconsistency between the manifest artifact and actual package contents.

### `delivery-5` — shoot.py silently writes a mislabeled screenshot when the nav guard cannot reach a hidden/skipped target frame
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** medium
- **Location:** `deck-json/shoot.py:164-178`
- **Problem:** The capture loop advances with ArrowRight until `_CUR_JS >= target0` OR the guard (`len(meta)+3`) is exhausted. If the target frame is hidden/conditional (ArrowRight steps over it and never lands on it) or the framework otherwise can't reach target0, the loop exits on guard exhaustion and then UNCONDITIONALLY screenshots the current (wrong) frame, saving it as p{target}-{key}.png and appending to `written`. There is no post-condition assertion that current == target0 before shooting. The tool then reports success (exit 0) for a screenshot that shows a different page than its filename claims. Since the skill mandates 'render then visually self-review the screenshots', a mislabeled shot actively misleads that review.
- **Evidence:**
  ```
  shoot.py:165  while page.evaluate(_CUR_JS) < target0 and guard < len(meta) + 3:
    166      page.keyboard.press("ArrowRight")
    ...
    171  page.wait_for_timeout(650)
    172  fn = out_dir / f"p{m['idx']:02d}-{m['key']}.png"
    ...  # no check that current actually reached target0
    178  written.append((m, fn))
  ```
- **Fix:** After the guard loop, re-read _CUR_JS; if it != target0, skip the shot, add the token to `missing`, and do not write the file (or write but warn loudly). At minimum print a per-page warning when guard exhausts so the operator knows the pixels may be the wrong frame, and make the exit code reflect it.

### `frontend-1` — Prev/Next button disabled-state mixes raw frame index with visible-slide count (breaks nav controls when hidden slides exist)
- **Severity:** MEDIUM  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/feishu-deck.js:1245-1246`
- **Problem:** updateUI() computes `cur = currentIdx(frames)` which returns the ABSOLUTE index in the frames array (0..frames.length-1), and `total = visibleCount(frames)` which counts only NON-hidden slides. It then sets `prev.disabled = cur <= 0` and `next.disabled = cur >= total - 1`. These two values live in different index spaces. When a deck has hidden (隐藏页 / data-hidden) slides that are not all at the tail, frames.length > total, so the comparison is wrong. Concretely, with the first two slides hidden (visible = frames 2,3,4; total=3): on the first VISIBLE slide (frame 2, visible position 1/3) `cur=2 >= total-1=2` evaluates true → the Next button is wrongly DISABLED even though two slides are still ahead, and `cur=2 <= 0` is false → Prev is wrongly ENABLED on the first visible slide. Keyboard/wheel/swipe nav still work (they use stepNext/stepPrev), so it is not a total lockout, but the on-screen pager controls are incorrect/confusing. The indicator and progress bar correctly use `pos`/`total`; only the button gating uses the wrong variable.
- **Evidence:**
  ```
  ui.querySelector('.ctl.prev').disabled = cur <= 0;
      ui.querySelector('.ctl.next').disabled = cur >= total - 1;
  ```
- **Fix:** Use the visible ordinal, consistent with the indicator: `const pos = visibleOrdinal(frames, cur);` then `prev.disabled = pos <= 1;` and `next.disabled = pos >= total;`. This matches the same `pos`/`total` already computed two lines above (lines 1231-1232).
- **Verifier:** Confirmed by reading assets/feishu-deck.js and simulating the logic. Lines 1245-1246 set `ui.querySelector('.ctl.prev').disabled = cur <= 0;` and `next.disabled = cur >= total - 1;`. The two operands are in different index spaces: `cur = currentIdx(frames)` (line 1228) returns the ABSOLUTE DOM-order index of the `.is-current` frame (lines 824-829, finding the frame with `.is-current` among `frames = Array.from(deck.querySelectorAll('.slide-frame'))` at line 454, pure document order, no reordering), while `total = visibleCount(frames)` (line 1231 → lines 857-859) counts only non-hidden frames (`isHidden` checks `.slide[data-hidden]`, lines 837-839). When hidden slides are leading or interspersed (not all at the tail), `frames.length > total`, so the absolute index overshoots the visible count. I reproduced the exact failure with a node snippet: 5 frames, first two hidden (visible=frames 2,3,4; total=3). On the first visible slide (cur=2, visible pos 1/3): buggy code gives prev.disabled=false (wrongly ENABLED) and next.disabled=(2>=2)=true (wrongly DISABLED), whereas correct is prev disabled, next enabled. The middle visible slide (cur=3) also gets next wrongly disabled. Reachability is real: line 632 `if (!readHash()) goTo(deck, frames, firstVisible(frames), false)` lands the initial current slide on the first non-hidden frame, which is a non-zero absolute index when slides are hidden up front, immediately triggering the wrong gating via updateUI (line 776/971). Lines 1245-1246 are the ONLY place prev/next .disabled is set (grep confirms), so no compensating logic. This is UI-only: the click handlers (lines 748-753) use stepPrev/stepNext which correctly skip hidden slides, and keyboard/wheel/swipe (lines 509/512/587/603) also use them, so there is no total lockout — but a disabled button suppresses its click handler, so the pager buttons become unclickable when wrongly disabled. The indicator and progress bar (lines 1236-1239) correctly use pos/total. The suggested fix is valid: pos and total are already computed at lines 1231-1232, so `prev.disabled = pos <= 1; next.disabled = pos >= total;` aligns the gating with the indicator's index space. Severity medium is honest: real correctness defect but conditional (only when hidden slides are not tail-only), UI-only, no data/corruption impact, graceful degradation via keyboard nav.

### `frontend-2` — Edit-mode save sanitizer does not strip balanceColumns runtime !important geometry → runtime values baked into index.html and folded into deck.json
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/edit-mode/deck-edit-mode.js:372-382`
- **Problem:** stripRuntimeInlineStyle() removes only `--child-i`, `--fs-scale`, and `top`/`bottom` when they carry `!important` (the _ccApply canvas-center signature). But the runtime's balanceColumns() in feishu-deck.js (lines 364-371) writes a whole block of RUNTIME-COMPUTED geometry with `!important`: flex:0 0 auto, min-height:0, height:auto, width:100%, background-size:cover, max-height:<Math.round(frameH)>px — all !important — plus aspect-ratio:<image natW>/<image natH> and the parent cell's justify-content:center !important. These values are derived at runtime from the loaded image's naturalWidth/naturalHeight and the scaled frame height; an author never writes them. Because they survive buildSavedHTML, a ⌘S save bakes them into index.html (the very R-BAKED-DOM / round-trip footgun the sanitizer was written to prevent — see the long comment at lines 327-361 which explicitly claims 'The ONLY compounding mutation is the canvas-center !important top/bottom, which we DO strip'). On a subsequent sync-index-to-deck.py these would be folded into deck.json as authored layout, compounding every round-trip ('越改越坏'). The discriminating signal already used for top/bottom — `!important` priority — also cleanly distinguishes balanceColumns (uses !important) from balanceSlide's deliberately-kept props (min-height/justify-content/align-self/padding written WITHOUT !important, verified at feishu-deck.js lines 127-172).
- **Evidence:**
  ```
  st.removeProperty('--child-i');
      st.removeProperty('--fs-scale');
      // top/bottom: only the !important (canvas-center) variant is runtime.
      for (const prop of ['top', 'bottom']) {
        if (st.getPropertyPriority(prop) === 'important') st.removeProperty(prop);
      }
  ```
- **Fix:** Extend the !important-priority strip loop to cover the balanceColumns props: `for (const prop of ['top','bottom','flex','min-height','height','width','background-size','max-height','aspect-ratio','justify-content']) { if (st.getPropertyPriority(prop) === 'important') st.removeProperty(prop); }`. (Stripping only the !important variants leaves any genuine author inline styles untouched, since those carry no priority.) Alternatively, gate the whole element by the data-fs-colbalanced marker, but the priority check is simpler and already the established mechanism.
- **Verifier:** Confirmed via direct code reading and grep. stripRuntimeInlineStyle (deck-edit-mode.js:372-382) strips only --child-i, --fs-scale, and !important top/bottom. balanceColumns (feishu-deck.js:343-383) writes runtime-computed geometry derived from the loaded image's naturalWidth/naturalHeight and scaled frameH: flex:0 0 auto, min-height:0, height:auto, width:100%, background-size:cover, max-height:<Math.round(frameH)>px, and the parent cell's justify-content:center — ALL with priority 'important' (lines 364-371), plus aspect-ratio WITHOUT important (line 367 — one inaccuracy in the claim, which listed aspect-ratio as !important). A full grep of feishu-deck.js confirms the ONLY runtime !important inline writes are the canvas-center top/bottom (lines 300-301, which ARE stripped) and these balanceColumns props; balanceSlide (lines 109-192) writes its kept props (alignSelf/justifyContent/minHeight/paddingTop/paddingBottom) via plain `style.x =` with no priority, so !important-priority is a clean discriminator. render-deck.py emits no !important inline styles (grep: the two hits are a comment and a stylesheet rule), so the suggested !important-gated strip would not delete any author output. The data-integrity chain holds: (1) buildSavedHTML/stripRuntimeArtifacts removes the three R-BAKED-DOM fingerprints that the gate checks (run-audits.py:815-851 only inspects data-idx / class=deck-ui / .deck runtime flags — never inline geometry), so a saved file with the leaked !important geometry passes the gate green; (2) sync-index-to-deck.py has the SAME regex gap (_RUNTIME_DECL_RE strips only --child-i/--fs-scale, _RUNTIME_CC_DECL_RE only top/bottom!important, lines 142-153), and at lines 1088-1099 folds a drifted raw slide's verbatim inner (carrying the runtime geometry) into deck.json data.html — the drift compare at line 1093 normalizes only asset paths/whitespace, so runtime geometry registers as a real author edit ("越改越坏"). The existing e2e test (test_edit_roundtrip_sanitize.py:379-409) asserts `'!important' not in markup` but its fixture _e2e_deck (lines 314-333) has NO background-size:contain letterboxed image column, so balanceColumns never fires and the assertion passes vacuously — the case is genuinely uncovered. The long comment at deck-edit-mode.js:357-361 ('The ONLY compounding mutation is the canvas-center !important top/bottom, which we DO strip') is therefore factually wrong. Severity medium is honest: reachable and defeats the sanitizer's stated purpose, but narrow trigger (a letterboxed contain-image media column with >18% vertical slack in a raw/imported slide), requires a save or sync to manifest, and corrupts layout geometry rather than causing catastrophic deck.json loss. The suggested fix (extend the !important-priority strip loop to flex/min-height/height/width/background-size/max-height/justify-content) is correct; note it should also add aspect-ratio explicitly since that prop is written WITHOUT !important and would otherwise still leak.

### `diff-1` — F-319 scope gate silently demotes in-scope R05 (emoji/'!'/'…') errors because their messages carry no "slide N" anchor
- **Severity:** MEDIUM  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/render-deck.py:3088-3093`
- **Problem:** F-319 re-runs `assets/validate.py --no-visual --json` and partitions static errors into in-scope vs off-scope by `e.get("slide") in scope_pages`. But validate.py derives `slide` purely by regex-parsing the literal substring `slide\s+(\d+)` out of each message (assets/validate.py:611,614,620 → `'slide': int(s.group(1)) if s else None`). The per-slide CONTENT rule R05 (audit_copy_bytes in assets/run-audits.py:668-702) scans the whole <body> at once and emits anchor-free messages like 'emoji detected in slide text' and "exclamation '!' / '！' detected in slide text" — no slide ordinal. So R05 errors parse to slide=None, land in `_off_scope`, and are DEMOTED: `_static_rc` is forced to 0 and the render is NOT rolled back (line 3093). Net effect: a scoped (or default auto-scoped — see line 2701-2704) edit that introduces a banned emoji / '!' / '…' / '???' onto an in-scope page passes the scoped static gate and ships the index.html. I confirmed via a Python snippet that `slide\s+(\d+)` returns None for all four R05 message variants while R02/R07 ('slide N: …') parse correctly. The comment at line 3072-3076 assumes only genuinely deck-level rules go to slide=null, but R05 is a per-slide rule implemented body-wide, so it is misclassified.
- **Evidence:**
  ```
  _serrs = json.loads(_jrc.stdout).get("errors", [])
  _sset = set(scope_pages)
  _in_scope = [e for e in _serrs if e.get("slide") in _sset]
  _off_scope = [e for e in _serrs if e.get("slide") not in _sset]
  if not _in_scope:
      _static_rc = 0   # in-scope page(s) are static-clean → don't roll back
  ```
- **Fix:** Don't treat ALL slide=null static errors as demotable deck-level noise. Either (a) maintain an explicit allowlist of truly deck-level static rule codes (R-KEY, R-SELF-CONTAINED, R-DOC-INTEGRITY, R-DOM, R-CSSVAR, R-DECK-*) and demote ONLY those, blocking on any slide=null error whose code is NOT in that set (so R05 still blocks); or (b) give R05 a real per-frame anchor — make audit_copy_bytes iterate `_extract_slide_frames` and prefix each finding with `slide {i}:` like R02/R-ESC-HTML already do, so the F-319 attributor can place it correctly. Option (b) is the cleaner long-term fix and also improves the standalone R05 diagnostic.
- **Verifier:** Every link in the claimed chain is verified against the actual code. (1) validate.py:611/614/620 derives the `slide` field by regex `slide\s+(\d+)\b` over the message string only. (2) R05 `audit_copy_bytes` (run-audits.py:688-702) emits anchor-free messages ("emoji detected in slide text", "exclamation '!' / '！' detected in slide text", "ellipsis '…' / '...'...", "'???' detected...") with slide_idx:0 and NO "slide N" ordinal in the message text — unlike R02 (line 652 "slide {i}: ..."), R07 (line 664), and R-KEY (line 571-594) which all embed the ordinal in the message despite also using slide_idx:0. The audits.js R05 (audits.js:2330-2353) emits the same anchor-free messages. (3) engine_findings_to_issues (validate.py:133-137) pours the message in verbatim with no slide-prefix injection. (4) I ran a Python snippet: all four R05 messages parse to slide=None (the "slide text" substring correctly does NOT match because of \d+/\b), while R02/R07/R-KEY parse to their real ordinals. (5) In render-deck.py:3082-3093 the demotion fires whenever scope_pages is truthy and not strict_baseline — and AUTO-SCOPE is the DEFAULT-ON entrance (line 2701, AUTO_SCOPE_MAX_PAGES=4), so this is the ordinary editing path, not just explicit --scope. (6) An in-scope R05 violation parses to slide=None → lands in _off_scope (line 3091) → if it is the only error, _in_scope is empty → _static_rc forced to 0 (line 3093) → the rollback at line 3493 and `return 4` are skipped, so the bad index.html ships. (7) The visual gate is NOT a backstop: the blocking set _vis_errors (line 3270-3273) only includes codes prefixed R-VIS/R-OVERFLOW/R-OVERLAP, so R05 is excluded there too. (8) The demoted R05 is even printed as "PRE-EXISTING, demoted (not this scoped edit's fault)" (line 3096-3104) — actively misleading, since it WAS introduced by this edit. The claim's evidence lines (3088-3093) match exactly. I corrected severity from high to medium because a real backstop exists: the mandatory --final whole-deck render has empty scope_pages, so line 3082's condition is False and R05 blocks normally before actual delivery — bounding the blast radius to scoped iteration renders. R05 is also a cosmetic content-policy rule (banned glyphs), not a data-corruption / crash / silent-data-loss class. The suggested fixes (allowlist truly deck-level static codes, or give R05 a per-frame anchor) are both sound; option (b) is the cleaner long-term fix.

### `diff-2` — F-319 scope-aware static gate (controls index.html rollback) ships with no regression test
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `deck-json/render-deck.py:3067-3116`
- **Problem:** The F-319 block decides whether a gate-failing scoped render rolls back its index.html (return 4 + `_rollback_index_html()`), which is on the data-integrity-adjacent path (it can let a render with static errors persist on disk). A grep of deck-json/tests for 'F-319', '_static_rc', 'scope-demoted', or 'in-scope page' returns nothing, and test_iteration_loop.py / test_auto_scope.py only cover happy-path returncode==0 renders — none seeds an off-scope static error and asserts it demotes, nor an in-scope static error and asserts it still blocks. So the demote-vs-block decision (and the R05 hole in diff-1) is entirely unlocked by tests. Existing suites still pass (I ran iteration_loop, auto_scope, atomic_render, baseline_gate, gate_tuning, audits_new_behaviors — all green), so no regression is introduced, but the new logic has no guard against future drift.
- **Evidence:**
  ```
  if not _in_scope:
      _static_rc = 0   # in-scope page(s) are static-clean → don't roll back
  ...
  if _static_rc != 0:    # F-319: scope-demoted on --scope (see above)
      ...
      _rollback_index_html()   # F-269: don't leave a gate-rejected file on disk
      return 4
  ```
- **Fix:** Add a test (alongside test_iteration_loop.py) that builds a 2-page deck, seeds a static error on an OUT-OF-SCOPE page and renders `--scope` the clean page → assert returncode 0 + index.html kept + the 'scope-demoted' advisory printed; and a second case seeding a static error on the IN-SCOPE page → assert returncode 4 + rollback. Include an R05-emoji-on-in-scope-page case to lock the diff-1 fix.

### `renderer-1` — --inline inlines data-src instead of the real src on <img>, leaving the displayed image external (single-file deck 404s after move)
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/render-deck.py:3858-3861`
- **Problem:** The media-inlining patterns use `<img\b[^>]*?\bsrc\s*=` with a non-greedy `[^>]*?` and a plain `\b` before `src`. For an <img> that carries a data-attribute ending in 'src' BEFORE the real src (e.g. `<img data-src="lazy.png" src="real.png">`), `\bsrc` matches the `src` inside `data-src` (the `-` is a word boundary), so the regex inlines `data-src` and leaves the REAL `src="real.png"` as an external link. The point of --inline is a portable single-file deck; here the actually-displayed image stays external and 404s the moment the deck is emailed/moved. Worse, the real ref is never visited so it is not recorded in `missing`, meaning --inline-strict does NOT catch it and a broken portable deck ships clean. Same flaw applies to the <source>/<video> src and <video> poster patterns for any data-* attribute. Confirmed by running the exact regex against `<img data-src="lazy.png" src="real.png">`.
- **Evidence:**
  ```
  r'(?P<pre><img\b[^>]*?\bsrc\s*=\s*)(?P<q>["\'])(?P<url>[^"\']+)(?P=q)(?P<post>)',
      r'(?P<pre><source\b[^>]*?\bsrc\s*=\s*)(?P<q>["\'])(?P<url>[^"\']+)(?P=q)(?P<post>)',
      r'(?P<pre><video\b[^>]*?\bsrc\s*=\s*)(?P<q>["\'])(?P<url>[^"\']+)(?P=q)(?P<post>)',
      r'(?P<pre><video\b[^>]*?\bposter\s*=\s*)(?P<q>["\'])(?P<url>[^"\']+)(?P=q)(?P<post>)',
  ```
- **Fix:** Anchor the attribute name with a negative lookbehind so a preceding word-char or hyphen prevents the match: replace `\bsrc` with `(?<![\w-])src` and `\bposter` with `(?<![\w-])poster`. Verified `(?<![\w-])src` skips `data-src` and still matches `<img src=...>` / `<img class="x" src=...>`. Add a --inline test fixture with `<img data-src=... src=...>`.
- **Verifier:** Mechanical defect CONFIRMED at deck-json/render-deck.py:3858-3861. The patterns use `<img\b[^>]*?\bsrc\s*=` with non-greedy `[^>]*?` and a plain `\b` before `src`. I ran the EXACT regex: input `<img data-src="lazy.png" src="real.png">` matches `pre='<img data-src='` and captures `url='lazy.png'`, leaving the real `src="real.png"` external. `\b` matches the `-`→`s` boundary and the non-greedy quantifier stops at the FIRST attribute name ending in `src`. Same flaw applies to <source src>, <video src>, and <video poster> for any `data-*` attribute ending in those names (notably `data-poster`, which the codebase's own copy-assets.py line 71 explicitly treats as a real asset ref). --inline-strict EVASION CONFIRMED: `missing` is only populated for VISITED refs via `_record_missing(url)` (line 3853); the real `src` is never visited, so a broken portable deck passes --inline-strict clean (lines 3613-3624 only fail when `_missing` is non-empty). Suggested fix VERIFIED: `(?<![\w-])src` skips `data-src`, still matches `<img src=...>` and `<img class="x" src=...>`. HOWEVER, severity is downgraded from high to medium on reachability grounds: the renderer's OWN canvas-image emitter (lines 1922, 1932) emits `<img loading="lazy" src="...">` — it uses `loading="lazy"`, NOT `data-src`, so deck.json-authored images never trigger this. The trigger requires raw-slide or imported HTML carrying `data-src` BEFORE `src` on the same <img>; that markup IS within the supported vocabulary (copy-assets.py and check-only.py list data-src/data-original/data-poster as first-class asset attrs and inline_html at line 3786 runs the regex over the whole rendered index.html including raw-slide html), but a grep of all repo decks found ZERO actual `<img data-src=... src=...>` instances (the only data-src in real files is on a `<style>` tag in a non-rendered prototype). So: a genuine correctness/data-integrity bug with a real silent-ship-clean failure mode on a reachable-but-currently-unexercised input class (raw/imported markup), not currently producible by the renderer's own output. Real bug, fix is correct, but high overstates current exposure.

### `renderer-2` — render --renumber silently deletes alphanumeric leading title tokens (5G/3D/24小时) and persists the loss to deck.json
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/render-deck.py:415`
- **Problem:** _renumber_label strips a leading page-number token with `^\s*\d[\w\-]*\s+`, intending to drop tokens like '01 ' or '04-A '. But `\d[\w-]*` matches any token that merely STARTS with a digit and is followed by word chars/hyphens, e.g. '5G', '3D', '2B', '24小时' — so a section titled '5G 时代的连接' becomes '时代的连接', '3D 建模工作流' becomes '建模工作流', '24小时 服务' becomes '服务'. Because --renumber writes the rewritten screen_label back into deck.json (the SSOT), this is persisted data loss (a .bak is taken, so recoverable). Confirmed by running the regex against realistic titles.
- **Evidence:**
  ```
  name = re.sub(r"^\s*\d[\w\-]*\s+", "", base).strip() or _derive_screen_label(slide)
  ```
- **Fix:** Only strip a PURE page-number ordinal, not an alphanumeric word. Use e.g. `^\s*\d{1,3}(?:-[A-Za-z0-9]+)?(?=\s)` (digits + optional `-A` page-variant suffix + whitespace) so '01 ', '50 ', '04-A ' are stripped but '5G ', '3D ', '24小时 ' are preserved. Add fixtures for digit+letter titles.
- **Verifier:** Confirmed at deck-json/render-deck.py:415. The regex `re.sub(r"^\s*\d[\w\-]*\s+", "", base)` strips any leading token that merely STARTS with a digit and is followed by word-chars/hyphens up to whitespace. Ran the regex: "5G 时代的连接"→"时代的连接", "3D 建模工作流"→"建模工作流", "24小时 服务"→"服务" (note: Python re `\w` matches CJK, so 24小时 is consumed), "2B 业务"→"业务", and even legitimate years "2024 年度回顾"→"年度回顾". The `or _derive_screen_label(slide)` fallback on line 415 only fires when the strip yields EMPTY, so a partial strip like "5G 时代的连接"→"时代的连接" (non-empty) survives and is NOT recovered. Persistence confirmed: --renumber block (lines 2786-2810) mutates slide["screen_label"]=new (line 2792) and writes the full deck back to deck.json (lines 2799-2800), so the loss is persisted to the SSOT. A .bak-pre-renumber-<ts> backup is taken first (lines 2797-2798) → recoverable. Schema check (deck-schema.json:122-125) confirms screen_label is a free-form string with NO numeric-prefix pattern constraint and "If omitted, derived from title", so a bare-title screen_label (no number prefix) is fully legal and reachable. IMPORTANT severity-calibration nuance the claim understates the safe path on: in the DOCUMENTED dominant convention where screen_label already carries a number prefix (e.g. "05 5G 时代的连接", per references/one-pager-case.md:171 and prototype-embed.md), the greedy `[\w\-]*` stops at the first whitespace so only "05 " is stripped → "5G 时代的连接" is preserved (verified). The data loss only triggers when screen_label is absent/derived OR authored as a bare title without a numeric prefix (Case B). Also "silently" is slightly imprecise: each change is echoed to stderr (lines 2803-2804). Net: a real, reachable data-integrity bug that mangles legitimate alphanumeric-leading titles in a metadata field (screen_label / pager-library label, NOT the on-screen rendered slide.data.title), gated behind an explicit --renumber flag, with a backup and a stderr echo. Medium is fair (not inflated; arguably could be low given backup+stderr+flag-gating+metadata-only, but persisting corrupted titles to the SSOT justifies medium). The suggested fix (anchor to a pure ordinal `^\s*\d{1,3}(?:-[A-Za-z0-9]+)?(?=\s)`) correctly preserves 5G/3D/24小时/2024 while still stripping 01/50/04-A.

### `renderer-3` — flow/swim drops a milestone when two milestones in a lane share the same quarter (last-write-wins, no warning)
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/render-deck.py:1645-1649`
- **Problem:** _enrich_flow_swim builds `milestones_by_q` keyed by quarter index; if a lane has two milestones with the same `quarter`, the second assignment overwrites the first, so the earlier milestone vanishes from the rendered grid with no stderr warning. The author sees fewer milestones than they wrote in deck.json, with no trace. Unlike other enrichers that render everything, this one silently discards content.
- **Evidence:**
  ```
  milestones_by_q = {}
  for mi, ms in enumerate(lane.get("milestones") or [], start=1):
      q = ms.get("quarter")
      if isinstance(q, int) and 1 <= q <= len(time_axis):
          milestones_by_q[q] = (mi, ms)
  ```
- **Fix:** Make the value a list (`milestones_by_q.setdefault(q, []).append((mi, ms))`) and stack multiple milestones in one cell, OR detect the collision and print a WARNING to stderr naming the lane+quarter so dropped content is never silent.
- **Verifier:** The cited code is exact. In _enrich_flow_swim (deck-json/render-deck.py:1645-1649) milestones are bucketed `milestones_by_q[q] = (mi, ms)` keyed by quarter index, then the render loop (lines 1650-1664) emits exactly one `<div>` per quarter column. So two milestones in the same lane sharing the same `quarter` collide last-write-wins: the earlier one is silently dropped. I reproduced this in isolation — input ['内测','公测','GA'] with two milestones at quarter 2 renders only '公测' at Q2; '内测' vanishes with no output and no stderr message.

Reachability is confirmed: the schema ($defs/data_flow_swim, deck-schema.json:251-264) requires quarter (integer 1..6) but has NO uniqueItems / uniqueness constraint on quarter within a lane, so a deck.json with two same-quarter milestones is schema-valid and passes the gate. The fragment template (templates/flow-swim.fragment.html) lays the stage out as a fixed CSS grid `grid-template-columns: 200px repeat(N,1fr); grid-template-rows: 60px repeat(lanes,1fr)` — there is structurally exactly one cell per (lane, quarter) intersection, so the model intrinsically assumes ≤1 milestone per cell; the collision is an authoring contradiction the schema fails to reject.

The "silent, no warning" characterization is fair: peer enrichers in the same file DO print `render-deck: WARN/WARNING` to stderr when content can't be rendered (line 1108 missing image, lines 1909/1927 unusable crop), so the swim enricher dropping authored content with zero trace is inconsistent with the established pattern.

Severity medium is honest, not inflated: this is genuine silent data loss of RENDER output, but (a) it is NOT corruption of deck.json — the SSOT retains both milestones and the loss is fully recoverable on a fixed render, and (b) it requires self-contradictory authoring (two tiles into one grid cell). The suggested fix is sound — either stack `setdefault(q, []).append(...)` and render multiple `.ms` tiles in the one cell div, or at minimum emit a stderr WARN naming lane+quarter so the drop is never silent (matching the file's own peer-enricher convention). Not critical (no deck.json corruption, no defeat of the optimistic lock/rollback), but a real medium data-integrity/robustness defect.

### `reskin-1` — find_main_container matches "slide" as a substring → selects decorative/page-number div, drops entire slide body
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** partially-confirmed  ·  **Confidence:** high
- **Location:** `assets/reskin.py:827-842`
- **Problem:** find_main_container picks the FIRST div whose class CONTAINS the substring 'slide' (or stage/canvas/deck) in document order. A tiny preceding element like <div class="slide-num">1</div>, <div class="slide-bg">, or a <div class="slideshow"> wrapper that appears before the real canvas root is matched and returned as the 'main container'. inner_html is then taken from only that element, silently discarding all real slide content. The canvas-size preflight is a separate CSS regex and gives no protection. Confirmed: a body with <div class="deck-root"><div class="slide-num">1</div><div class="content">real content</div></div> yields main=.slide-num and inner_html="1".
- **Evidence:**
  ```
  for sel_class in ("slide", "stage", "canvas", "deck"):
      for el in body.find_all("div"):
          classes = " ".join(el.get("class", [])).lower()
          if sel_class in classes:
              return el
  ```
- **Fix:** Prefer an exact class-token match over substring: check `sel_class in el.get('class', [])` (list membership), and among matches prefer the outermost / largest-subtree container (e.g. the one with the most descendants or that is an ancestor of the others) rather than the first in document order. Fall back to the substring heuristic only if no exact match exists.
- **Verifier:** The core mechanism is real and I reproduced it. find_main_container (reskin.py:833-837) iterates sel_class in ("slide","stage","canvas","deck") and returns the FIRST div whose joined class string CONTAINS the substring (substring `in classes`, not exact token membership), in body.find_all("div") preorder. At the call site (line 1321) inner_html = main.decode_contents() takes inner HTML from ONLY that element, so a wrong small match silently drops sibling/cousin content — violating the module's stated hard rule (line 13-17: "source's actual content layout survives byte-for-byte"). I ran the claim's exact fixture (deck-root > slide-num"1" + content): matched .slide-num, inner_html="1". I also confirmed a realistic silent-loss variant: a .slide-num"1" sibling appearing before a non-slide-ish .page content root yields inner="1" and the empty-guard at line 1335 (`if not inner_html.strip()`) does NOT fire. The canvas preflight (line 1188-1224) is purely CSS-regex (detect_source_canvas) and gives no protection, as claimed. Single definition, single call site, no test coverage.

Two corrections downgrade it from the claimed "high" to medium: (1) The claim's empty examples (.slide-bg, empty .slideshow) actually trigger the empty-guard SystemExit("source HTML has no usable body content") — a LOUD failure, not silent loss; I verified an empty .slide-bg sibling hits that guard. Only NON-empty wrong matches (page numbers, slide-ish wrappers with text) cause truly silent data loss, so the claim overstates the vector list. (2) Reachability is conditional: the canonical/documented feishu input has .slide AS the canvas root (confirmed at scope_selectors comment lines 593-607); in preorder the ancestor .slide is matched first, so the common path is SAFE. The bug only triggers on non-canonical foreign HTML where a slide-substring decorative/numbering div precedes a content root that itself lacks all four substrings. reskin advertises "any" foreign HTML (line 4), so it is genuinely reachable, but it is not the typical input — hence medium, not high. The suggested fix (prefer exact class-token membership via `sel_class in el.get('class', [])`, then choose the outermost/largest-subtree container, substring as last resort) is correct.

### `reskin-4` — scope_selectors does not scope @supports (and other braced at-rules) → CSS leaks across all slides
- **Severity:** MEDIUM  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/reskin.py:523-531`
- **Problem:** scope_selectors only recurses into @media; @keyframes/@font-face are intentionally left alone, but ANY other braced at-rule (@supports, @container, @layer) falls into the final else and is re-emitted with its inner selectors UN-scoped. The whole purpose of scope_selectors is to confine rewritten foreign CSS to .slide[data-slide-key=K]; an @supports block's selectors therefore leak globally onto every slide in the deck. Confirmed: '@supports (display:grid){ .card{color:red} }' passes through with .card unprefixed.
- **Evidence:**
  ```
  if atname in ("keyframes", "-webkit-keyframes", "font-face"):
      out.append(f"{atprelude}{{{body}}}")
  elif atname == "media":
      scoped_body = scope_selectors(body, slide_key, warnings)
      out.append(f"{atprelude}{{{scoped_body}}}")
  else:
      out.append(f"{atprelude}{{{body}}}")
  ```
- **Fix:** Recurse for any conditional-group at-rule that contains style rules: add @supports, @container, @layer (with a block body) to the @media branch so their inner selectors are scoped. Keep @keyframes/@font-face/@page in the pass-through branch.
- **Verifier:** Confirmed at assets/reskin.py:481-539. The cited lines 523-531 match exactly: scope_selectors special-cases @keyframes/-webkit-keyframes/font-face (pass-through, line 523-525) and @media (recurses, line 526-529), and dumps every OTHER braced at-rule into the final else (line 530-531) which re-emits `{atprelude}{{{body}}}` with the body's inner selectors UN-scoped.

Reproduced behaviorally: I loaded reskin.py and ran scope_selectors on `@supports (display:grid){ .card{color:red} } @media (max-width:600px){ .box{color:blue} } @container (min-width:200px){ .panel{color:green} }`. Output: the @media .box was correctly rewritten to `.slide[data-slide-key="S1"] .box`, but @supports `.card` and @container `.panel` stayed bare — exactly the leak the reviewer claims.

Reachability into final output confirmed: compose_data_html (reskin.py:920-985) embeds the scoped CSS as a `<style>` block INSIDE the raw slide's data.html (line 977-979), and reskin emits a layout:"raw" slide (line 1451). At render time, render-deck.py only runs the (correct) _css_utils.scope_selectors on the `custom_css` field via _inject_custom_css (render-deck.py:2074-2097, scope at 2081) — it does NOT re-scope `<style>` blocks embedded in raw data.html. So there is no second pass to catch the leak; the un-scoped @supports/@container selectors survive verbatim into index.html and apply globally across every slide once the reskinned slide is lifted/pasted into a multi-slide deck.

Contributing root cause: reskin.py has its own DUPLICATE scope_selectors (0 references to _css_utils — verified by grep). The canonical deck-json/_css_utils.py version handles these correctly via `_AT_NESTED = {"media","supports","container","layer","scope"}` (line 106, used at line 202), with a docstring explicitly listing @supports/@container/@layer as recursed. reskin's copy diverged. No reskin test covers @supports/@container/@layer (grep found those tokens only in audits.js / _validate_common.py, not reskin tests), so the gap is untested.

Severity medium is honest, not inflated: it is a real correctness/CSS-leak bug producing wrong output, but bounded — it needs foreign source HTML using @supports/@container/@layer with bare selectors (uncommon; reskin is a one-off foreign-import tool), and it is not corruption/crash/data-loss. Suggested fix is correct: add supports/container/layer (block-bodied) to the recurse branch alongside @media (ideally just reuse _css_utils.scope_selectors to kill the divergent duplicate); keep @keyframes/@font-face/@page as pass-through.

### `reskin-5` — rebundle-import script-src regex misses <script> tags with extra attributes or query string → silently skips re-bundle
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `assets/rebundle-import.py:60`
- **Problem:** The linked-runtime detector requires the script tag to be exactly <script src="...feishu-deck.js"></script> with no other attributes and no query string. A tag with defer/type=module/crossorigin, or a cache-bust src like feishu-deck.js?v=2, does not match. The code then falls to the inline branch, finds no inline runtime, and prints '⚠️ 没找到内联框架 runtime <script>(未 re-bundle JS)' — the tool's primary function (swap in the current runtime) silently no-ops while exiting 0. Confirmed: '<script src="feishu-deck.js" defer></script>' and '...feishu-deck.js?v=2' both fail to match.
- **Evidence:**
  ```
  link_m = re.search(r'<script\s+src="([^"]*feishu-deck\.js)"\s*>\s*</script>', html)
  ```
- **Fix:** Loosen the regex to allow other attributes and an optional query/fragment, e.g. r'<script\b[^>]*\bsrc="([^"?#]*feishu-deck\.js)(?:[?#][^"]*)?"[^>]*>\s*</script>'. Capture only the path portion for the path-traversal guard.

### `reskin-6` — title_text / subtitle_text interpolated unescaped into HTML → broken markup on titles containing & < >
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `assets/reskin.py:987-1032`
- **Problem:** compose_data_html injects title_text (from foreign get_text()) directly into <h2 class="title-zh">{title_text}</h2> and subtitle_text into <p class="page-sub">{subtitle_text}</p> with no HTML escaping. A perfectly legitimate source title like 'A & B' produces invalid HTML, and any '<'/'>' in the extracted text breaks the surrounding markup. Confirmed: title 'A & B <script>alert(1)</script>' is emitted verbatim into the h2. Since the input is a foreign HTML file the user chose to reskin, this is primarily a correctness/robustness bug (malformed output) with a latent injection flavor.
- **Evidence:**
  ```
  f'  <h2 class="title-zh">{title_text}</h2>\n'
  ...
  f'<p class="page-sub">{subtitle_text}</p>' if subtitle_text else ""
  ```
- **Fix:** Escape with html.escape(title_text) / html.escape(subtitle_text) when interpolating into the .title-zh and .page-sub elements (the JSON fields data.title / screen_label are fine as-is since json.dumps handles them).

### `subskill-6` — Importer reuse path can stamp a stale/unrelated audit verdict onto a different --html artifact
- **Severity:** MEDIUM  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** medium
- **Location:** `subskills/importer/ingest.py:124-131,666-667`
- **Problem:** ensure_quality_gate reuses 'existing validator PASS evidence' when audit_passed(output_dir) is true, but audit_passed only inspects output_dir/audit-report.json|AUDIT_REPORT.md — it never confirms that evidence pertains to the html_path actually being ingested. In the `--task-id <run> --html <other.html>` flow, output_dir's audit report corresponds to that run's index.html, not the external --html, yet the gate returns ok/reused and skips re-running check-only against --html. This can ingest an unvalidated artifact into the slide library under a stale PASS.
- **Evidence:**
  ```
  if audit_passed(output_dir):
          return {"ok": True, "reused": True, ... "reason": "existing validator PASS evidence reused"}
  ```
- **Fix:** Only reuse the audit verdict when the resolved html_path is the run's own output_dir/index.html (i.e. args.html is None or resolves to output_dir/index.html). When an external --html is supplied, always run check-only --gate ingest against that exact file instead of reusing output_dir evidence.
- **Verifier:** Confirmed and reproduced. audit_passed() (ingest.py:94-110) only checks for output_dir/audit-report.json or output_dir/AUDIT_REPORT.md with a PASS verdict and has NO linkage to which HTML the verdict pertains to. ensure_quality_gate() (lines 124-131) short-circuits on audit_passed(output_dir) returning {ok:True, reused:True} WITHOUT running check-only --gate ingest against html_path. The parser (lines 576-577) does not make --task-id and --html mutually exclusive, and resolve_html (lines 161-162) prioritizes args.html over output_dir/index.html. So with `--task-id <run> --html <other.html>`, html_path = the external file while output_dir = the run's dir whose audit-report.json pertains to the run's OWN index.html. The external unvalidated html_path is then handed to ingest_with_slide_library (lines 666,674). I reproduced this end to end with a tiny script: with a PASS audit-report.json in output_dir and an unrelated external.html, the gate returned ok=True/reused=True and never ran check-only against external.html — so an unvalidated artifact would be ingested into the slide library under a stale PASS. The audit report files are external artifacts (no Python in the repo writes them; only ingest.py and publisher/publish.py read them — publish.py:106-120 has the identical pattern), confirming they are dropped into a run's output_dir by the audit/designer flow and are run-specific. Severity held at medium (not higher): the two DOCUMENTED flows are safe — `--html`-only always builds a fresh timestamp-stamped output_dir (lines 623,629-630) with no pre-existing report so the gate correctly runs against --html, and `--task-id`-only legitimately validates the run's own index.html. The defect is only reachable via the parser-permitted but undocumented mixed combination, i.e. operator misuse; but the consequence (silently ingesting unvalidated content into a shared library under a stale PASS) is a genuine data-integrity violation of the project's stated invariants. The reviewer's proposed fix is correct: only reuse the verdict when html_path resolves to output_dir/index.html; otherwise always run check-only --gate ingest against the supplied --html.

### `subskill-4` — Post-publish self-check passes vacuously (ok=true) when no slides pair up or all screenshots fail
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `subskills/publisher/self_check.py:400-427`
- **Problem:** compare_captures computes ok = not broken_unique and not font_fallbacks and not changed. Slides that cannot be paired by key (local vs remote derive different keys, e.g. a baked DOM) or whose screenshots are empty go into the `missing` list, which does NOT factor into ok. So if zero slide pairs match (all missing) and there were no failed network requests, the self-check reports ok:True having verified nothing on the visual/font dimensions. The companion test (test_compare_missing_pair_reported_not_crashed) confirms missing pairs are merely reported, not failed. A verification gate that verified nothing should not report green.
- **Evidence:**
  ```
  ok = not broken_unique and not font_fallbacks and not changed
  # `missing` (unpaired / no-screenshot slides) never influences `ok`
  ```
- **Fix:** Treat an empty matched-pair set as a non-pass (or at least a soft red): if no slide produced a real visual comparison AND no broken-link evidence was collected, set ok=False with a reason like 'self-check produced no comparable slide pairs'. Optionally flag any `missing` entry caused by screenshot capture errors as a failure rather than silent.

### `subskill-5` — Importer/publisher network subprocesses run without a timeout (git clone / node publish can hang indefinitely)
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `subskills/importer/ingest.py:189-216,389,460`
- **Problem:** subprocess_record() in both importer/ingest.py and publisher/publish.py calls subprocess.run without a timeout. The importer runs bootstrap-library.py (which clones https://github.com/FuQiang/feishu-slide-library.git over the network) and the publisher runs `node magic-page-publish.js` (network upload to Magic Page). A stalled network connection hangs the whole run with no bound. Contrast with parse.py, where every lark-cli call uses timeout=60/600. These last-mile tools are the most likely to touch a flaky network.
- **Evidence:**
  ```
  def subprocess_record(cmd, *, cwd, log_path=None, env=None):
      proc = subprocess.run(cmd, cwd=cwd, text=True, capture_output=True, env=env)
      # no timeout=
  ```
- **Fix:** Add a timeout parameter to subprocess_record (default e.g. 300-600s) and wrap subprocess.run in try/except subprocess.TimeoutExpired, returning an ok:False step with a timeout reason; pass a generous timeout for the git-clone/node-publish steps.

### `sync-1` — Canvas reverse-map drops per-run size/font/grad — silent typography corruption on any canvas sync
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** partially-confirmed  ·  **Confidence:** high
- **Location:** `deck-json/sync-index-to-deck.py:306-323 (_runs_from_inner), 384-390 (sync_canvas_data text branch)`
- **Problem:** _runs_from_inner reconstructs each run from only {text, bold (via "700" in style), color}. But render-deck (render-deck.py:1819-1855) emits runs that also carry font-size (run.size→`font-size:Ncqw`), font-family (run.font), and gradient (run.grad). On a canvas sync, the text branch (`if runs: el["runs"] = runs`) OVERWRITES the existing runs with these stripped versions, permanently deleting size/font/grad. pptx-to-deck (build_pptx.py:743-758) sets size/color/grad/font/bold on virtually every run, so a single browser edit that triggers a full sync silently strips per-run typography from EVERY text element on EVERY canvas slide (not just the edited one — the canvas branch runs for all canvas slides and every run differs). Verified: a run {text:'Title',size:48,bold:true,font:'Arial'} round-trips to {text:'New Title',bold:true} after a text-only edit. The test fixture only uses text/bold/color runs, hiding this; test_roundtrip_stable_second_sync would fail with a size/font run.
- **Evidence:**
  ```
  run = {"text": _text_from_html(text)}
          if "700" in style:
              run["bold"] = True
          if cm:
              run["color"] = cm.group(1).strip()
          runs.append(run)   # <-- never recovers size / font / grad
  ```
- **Fix:** Parse font-size (Ncqw → px via /100*W), font-family (strip the appended fallback chain), and the gradient/background-image from each span's style and re-emit them on the run; OR (more robust) MERGE recovered fields onto the existing run dicts positionally instead of replacing the runs list, so any field the reverse-parser doesn't understand survives. Add a test where _canvas_slide() runs carry size/font/grad and assert second-sync stability.
- **Verifier:** The MECHANISM is real and I reproduced it. _runs_from_inner (sync-index-to-deck.py:306-323) recovers only {text, bold via "700", color}; render-deck.py:1827-1854 emits per-run font-size (Ncqw), font-family, and background-image(grad). The canvas text branch (lines 387-390) overwrites el["runs"] with the stripped version. Empirical repro: a run {text:"Title",size:48,bold:true,font:"Arial",grad:"linear-gradient(...)",color:"#1A1A1A"} renders WITH font-size/font-family/background-image, but after a text-only edit sync_canvas_data returns {"text":"New Title","bold":true,"color":"#1A1A1A"} — size, font, and grad are dropped. The test fixture _canvas_slide() (test_canvas.py:47-55) uses only text/bold/color, so test_roundtrip_stable_second_sync genuinely fails to cover size/font/grad, exactly as claimed. So the reverse-parser is lossy and the coverage gap is real.

HOWEVER the claimed severity/framing is materially overstated. The claim says "a single browser edit that triggers a full sync silently strips typography from EVERY canvas slide." That is NOT what the automated pipeline does. The codebase has an explicit F-315 guard: when a canvas slide drifts, sync sets lossy_drift=True (line 1080) and --check-drift returns exit code 11 (lines 1181-1189). _index_sig.resolve_clobber maps code 11 to ("refuse",...) (_index_sig.py:229-233). Both automated writers refuse rather than auto-sync canvas edits: deck-cli.py:1378-1385 returns 6 ("REFUSING ... Or pass --force to ... DISCARD"), and render-deck.py:2600-2608 returns 8. Only code 10 (all-lossless: raw/custom_css/order/hidden/notes) triggers auto_sync. So no automated path silently folds the stripped canvas data into deck.json.

The loss only occurs when a HUMAN explicitly runs sync-index-to-deck.py on a canvas deck (then lines 1081-1082 write new_data). That is precisely the documented-lossy manual operation: line 308 calls it a "documented lossy boundary," and test_roundtrip_multirun_flatten_is_lossy already locks canvas reverse-sync as known-lossy. So this is a real data-integrity defect (silent field loss + a hiding test fixture) but gated behind a manual-only path protected by an explicit guard — not "critical corruption on any sync." Severity medium. The suggested fix (positionally MERGE recovered fields onto existing run dicts so unknown fields survive, plus a size/font/grad fixture in test_roundtrip_stable_second_sync) is correct and worth doing.

### `sync-3` — Backfill EXACT path duplicates first slide's content for any repeated data-slide-key (silent data loss)
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/sync-index-to-deck.py:578-605 (native backfill loop), 224-234 (extract_slide_inner uses re.search → first match)`
- **Problem:** When index.html has duplicate data-slide-key values (the loop explicitly anticipates this — see the dedup), the dedup only RENAMES the key (dup→dup-2). But extract_slide_inner, _screen_label_for, and _extract_slide_custom_css all locate the slide by `re.search(... data-slide-key="K" ...)`, which always returns the FIRST occurrence. So the second (and later) duplicate-keyed slides get the FIRST slide's inner HTML, label, and custom_css. The second slide's actual content is silently lost while the warning only mentions a 'rename for uniqueness', masking the data loss. Verified: two slides keyed 'dup' with distinct bodies both backfill to the first body. This breaks the EXACT/lossless contract the native backfill path advertises.
- **Evidence:**
  ```
  ukey = key
              if ukey in used:
                  ukey = _make_key(key, used, n)
                  warnings.append(f"duplicate data-slide-key {key!r} → renamed ...")
              ...
              inner = extract_slide_inner(index_html, key)  # always FIRST occurrence of `key`
  ```
- **Fix:** Extract slide inner by DOM POSITION (Nth .slide), not by key lookup — e.g. iterate the slide-open matches positionally and depth-extract each, so each duplicate-keyed slide carries its OWN content; then dedup keys. Also escalate the duplicate-key warning to flag that bodies could not be distinguished if positional extraction is not adopted.
- **Verifier:** Empirically reproduced. backfill_deck() at sync-index-to-deck.py:578-605 iterates _slide_keys_in_dom_order() (which is _SLIDE_OPEN_RE.findall at line 426-432, returning EVERY key in DOM order including duplicates), but resolves each slide's content with extract_slide_inner(index_html, key) (line 579), _screen_label_for(index_html, key) (599) and _extract_slide_custom_css(index_html, key) (602). All three locate the slide via re.search(... data-slide-key="K" ...) — extract_slide_inner line 230-231, _screen_label_for line 441, _extract_slide_custom_css line 457 — which always returns the FIRST occurrence. So a 2nd/Nth slide sharing a key gets the FIRST slide's inner HTML/label/CSS; its own body is lost. The dedup at 586-592 only renames the key (dup→dup-2) and the warning at 589-590 says merely 'renamed for uniqueness', masking the content substitution.\n\nMy repro (two .slide divs both keyed 'dup' with bodies 'FIRST BODY' and 'SECOND BODY DISTINCT') produced: KEY dup -> 'FIRST BODY', KEY dup-2 -> 'FIRST BODY' (the second body silently dropped), with only the rename warning. Confirmed.\n\nSeverity adjusted from high to medium: (1) reachability is restricted to input index.html with duplicate data-slide-key, which is an R-KEY ERROR (assets/audits.js:1815-1822) and cannot be produced by the normal render pipeline — it only arises from malformed/hand-edited/lift-stitched/foreign HTML, i.e. exactly the recovery scenario backfill targets, and the code itself anticipates it ('a malformed deck could repeat one', line 584); (2) it is not strictly silent — a warning DOES print (run_backfill line 662-663) and run_backfill backs up any existing deck.json (.bak-pre-backfill, line 674-678) and prints a 'verify parity' next-step that would surface the discrepancy. So the data loss is real and the warning is misleading (rename ≠ content substitution), but mitigations and the invalid-input gate keep this off the mainline. Every technical detail of the claim (cited lines, first-match root cause, affected functions, misleading warning, positional-extraction fix) is accurate.

### `sync-4` — Newly-added canvas image/shape elements lose src / svg on reverse-map
- **Severity:** MEDIUM  ·  **Category:** data-integrity  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/sync-index-to-deck.py:367-382 (sync_canvas_data add branch)`
- **Problem:** When the HTML carries a data-el-id not present in deck.json (the documented 'add' case), the reverse-map builds new_el = {id, type, +geometry} and only recovers runs for text. For type=image the src is never parsed from the <img src=...>, and for type=shape the <svg> path/inner is never captured. The result is an image element with no src (renders to a broken/empty box) or a shape with no svg. Verified: an added <img data-el-id=newimg src=input/pic.png ...> reverse-maps to {id,type:image,geometry} with no src. Lower severity than sync-1 because adding canvas elements via the browser DOM (rather than deck.json) is uncommon, but when it happens the asset reference is silently dropped — and a dropped asset at delivery is a real bug.
- **Evidence:**
  ```
  new_el = {"id": eid, "type": etype}
              new_el.update(_canvas_geom_from_style(h["style"], W, H))
              if etype == "text":
                  new_el["runs"] = _runs_from_inner(h["inner"])
              # img: no src parsed; svg: no path/inner parsed
  ```
- **Fix:** In the add branch, for img parse src (and crop/object-fit if present) from the open tag; for svg capture el_inner (already collected as h['inner']) into el['svg']. At minimum, emit a warning that an added image/shape could not have its content recovered so the operator does not get a silently broken element.
- **Verifier:** I read deck-json/sync-index-to-deck.py:350-395 (sync_canvas_data) and 326-347 (_collect_canvas_els), and empirically reproduced the bug. The add branch (lines 367-383) builds new_el = {"id", "type"} + geometry and ONLY recovers content for text (line 379-380: `if etype == "text": new_el["runs"] = ...`). It never parses src for image nor svg for shape. My runnable test (loading the real module, feeding an inner with an added `<img data-el-id="newimg" src="input/pic.png">` and an added `<svg data-el-id="newsvg"><path .../></svg>`) produced elements {id,type:image,x,y,w,h} with `image has src? -> False` and {id,type:shape,...} with `shape has svg? -> False`. Notably _collect_canvas_els DOES capture the svg's inner into h['inner'] (`<path d="M0 0 L100 100" .../>`), so the shape content is available but discarded; the img src lives in the open tag but _collect_canvas_els only extracts style="..." (_STYLE_RE), so src is not captured at all. Downstream, render-deck.py:1884 does `src = html.escape(str(el.get("src","")))` → a missing src renders `src=""` (broken empty box) with NO crash/warning; a shape with no svg renders an empty `<div class="el shape">` (render-deck.py:1968-1972) — silent content/asset loss, exactly as claimed. Reachability: the add case is documented (comment lines 270-279, "an HTML data-el-id with no matching JSON element = add") and the in-repo test test_roundtrip_add_element (tests/test_canvas.py:198) ONLY exercises an added TEXT element — there is no test for add-image/add-shape, so the prototype's "8/8 add lossless" claim was never validated for these types. sync_canvas_data is wired into the live sync flow (called at line 1075). Severity stays medium (not higher) for two reasons I verified: (1) any canvas drift sets lossy_drift=True (line 1080), so the auto-sync gate `--check-drift` returns exit 11 ("needs human/--force", lines 1186-1189) and the default deck-cli/render auto-sync path will NOT silently apply it; (2) even a direct non-dry-run invocation that writes the bad element (lines 1210-1212) first makes a .bak-pre-sync backup (line 1207), so it is recoverable — not unrecoverable corruption. But adding a canvas element via the DOM is a documented supported path and a dropped image/shape at delivery is a genuine silent-data-loss bug. The suggested fix is correct: in the add branch, parse src (and crop/object-fit) for img — which also requires extending _collect_canvas_els to capture more of the img open tag than just style — and store h['inner'] into new_el['svg'] for shape; at minimum warn that an added image/shape could not have its content recovered.

### `sync-5` — reconcile-reflow writes deck.json with no post-write schema validate/rollback (bypasses single-writer discipline)
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** medium
- **Location:** `deck-json/reconcile-reflow.py:636-649 (final write), contrast conform-to-deck.py:680-691`
- **Problem:** reconcile-reflow mutates the in-memory deck (font-snap of data.html via reconcile-lifted, plus appended custom_css rules) and writes deck.json directly with only a .bak and an mtime optimistic lock. Unlike conform-to-deck.py (which runs validate-deck --strict after writing and rolls back byte-for-byte on failure) and deck-cli's write_deck_with_validation, this path has NO post-write schema validation and NO rollback. The architecture invariant is that deck-cli is the single writer with schema-fail rollback; an ad-hoc writer that skips validation can land a schema-invalid deck.json if reconcile-lifted's HTML mutation ever produces malformed content. Risk is moderated because each round re-renders the in-memory deck successfully (gross corruption would surface), but the safety net is absent at the actual on-disk write.
- **Evidence:**
  ```
  args.deck_json.write_text(
          json.dumps(deck, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
      say(f"\n  ✓ backup: {bak.name}")
      say(f"  ✓ wrote {args.deck_json}")   # no validate-deck --strict + rollback after this
  ```
- **Fix:** After the final write, run validate-deck.py --strict on the result and restore from the .bak on non-zero exit (mirror conform-to-deck.py:680-691), or route the write through deck-cli's write_deck_with_validation so the rollback + lint guarantees apply.

### `misc-1` — deck-map.py deck.json index does not skip _disabled slides → disagrees with frame_index / URL #N
- **Severity:** MEDIUM  ·  **Category:** contract-drift  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/deck-map.py:107-123`
- **Problem:** map_deckjson() enumerates ALL slides 1..N including those marked _disabled, so its reported index is the raw array position. But the canonical rule (stated in locate-slide.py and enforced by the renderer) is page N = frame_index N = position AFTER skipping _disabled slides: render-deck.py builds slide-index.json from `active = [s for s in slides if not s.get('_disabled')]` and locate-slide.py's deck.json fallback does `if s.get('_disabled'): continue`. Result: for any deck containing a _disabled slide, deck-map's index for every slide after it is off-by-one (or more) versus the real page number, the URL #N, slide-index.json, and locate-slide. deck-map exists precisely to give the authoritative page map ('so you never have to archaeology a montage by hand again'), so a wrong page number here is a direct correctness failure. The HTML-input path is unaffected (rendered HTML already omits _disabled frames).
- **Evidence:**
  ```
  def map_deckjson(deck: dict) -> list[dict]:
      rows: list[dict] = []
      for n, s in enumerate(deck.get("slides", []), 1):   # <- counts _disabled slides too
          ...
          rows.append({"index": n, "key": s.get("key"), ...})
  
  # vs render-deck.py: active = [s for s in deck.get('slides', []) if not s.get('_disabled')]
  # vs locate-slide.py: if s.get('_disabled'): continue
  ```
- **Fix:** Skip _disabled slides when assigning the index in map_deckjson, mirroring the renderer: iterate with a separate frame counter that only increments for non-_disabled slides (and either omit _disabled rows or render them with index None / a '—' marker, like locate-slide treats hidden). This makes deck-map's index equal frame_index for deck.json input.
- **Verifier:** Confirmed and reproduced. deck-map.py:107-123 map_deckjson() does `for n, s in enumerate(deck.get("slides", []), 1)` and sets "index": n = raw array position with NO _disabled skip; pages=len(rows) also over-counts. The canonical page/frame contract excludes _disabled: render-deck.py:2768-2769 renders only active_slides=[... if not s.get("_disabled")] with total=len(active_slides) (line 2780); slide-index.json is built from the same active filter (line 2357); locate-slide.py:117-131 deck.json fallback does `if s.get("_disabled"): continue` with a separate fi counter. I ran a 4-slide deck with slide 2 _disabled: deck-map.py --json reports p3=index3, p4=index4, pages=4, while locate-slide.py resolves p3 to #2 (frame_index 2) and the real total is 3. So every slide after a _disabled one is off-by-one (or more) and the count is wrong vs URL #N / slide-index.json / renderer total. The claim correctly limits the bug to the deck.json path: map_html is unaffected because the renderer omits _disabled frames from the DOM (active_slides only), so scanning slide-frame divs already yields post-skip numbering. The claim also correctly scopes it to _disabled and NOT hidden — hidden slides ARE rendered and counted by both deck-map and locate-slide (locate-slide.py:119 comment confirms hidden is counted), so hidden is not a divergence source. _disabled is a real, durable, schema-documented field (deck-schema.json:166-169). The off-by-one is genuine and reachable. I downgrade claimed high to medium: deck-map is a read-only reporting tool (no data corruption / no deck.json write), and the bug only triggers on decks containing a _disabled slide fed via deck.json rather than rendered HTML; still a real correctness/contract-drift failure that defeats deck-map's stated purpose of being the authoritative page map. Suggested fix (separate frame counter mirroring locate-slide.py) is correct.

### `misc-3` — translation-qa residual-cjk flags CJK inside <script> bodies (e.g. fs-deck-notes speaker-notes island) as HARD untranslated findings the pipeline never translates
- **Severity:** MEDIUM  ·  **Category:** contract-drift  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/translation-qa.py:61-94`
- **Problem:** cmd_residual blanks <!-- --> and /* */ comment bodies, url(), font-family, and attribute URL refs, but NOT <script> or <style> JS/JSON bodies. Any CJK inside a <script> is reported as a HARD 'untranslated Chinese' finding (rc=1). The rendered index.html that the translator workflow scans (references/translation.md: residual-cjk runs on index.html and iframe copies) embeds `<script type="application/json" id="fs-deck-notes">` containing the deck's speaker notes verbatim (render-deck.py:2904-2908) — which in a Chinese deck are Chinese and are intentionally NOT translatable via text-swap (notes live in slide.notes, never in data.html, and extract-text-pairs explicitly skips <script>). So residual-cjk hard-fails on a deck the extract/apply pipeline considers fully translated, with no actionable fix. This is a direct disagreement with extract-text-pairs' RunExtractor which skips script content (_skip_depth).
- **Evidence:**
  ```
  # cmd_residual masks comments + url/font/attrs but never script/style bodies:
  text = re.sub(r'<!--.*?-->', blank, text, flags=re.S)
  text = re.sub(r'/\*.*?\*/', blank, text, flags=re.S)
  # ... no <script>...</script> blanking ...
  # render-deck.py emits Chinese speaker notes inside a script:
  '<script type="application/json" id="fs-deck-notes">' + json.dumps(notes_map, ensure_ascii=False)...
  # extract-text-pairs RunExtractor: if tag == 'script': self._skip_depth += 1  (script CJK never extracted)
  ```
- **Fix:** Blank <script>...</script> bodies in cmd_residual the same way comments are blanked (keep newlines for line numbers), matching extract-text-pairs' decision that script content is non-translatable; or special-case known non-translatable islands (id=fs-deck-notes). If some script-embedded CJK IS meant to be visible (rare), surface it as a soft note rather than a HARD fail.
- **Verifier:** Verified the full chain end-to-end, including an executable reproduction.

1) cmd_residual (translation-qa.py:61-110) and its helper _mask_nonvisible (37-58) blank only: <!-- --> comments (line 71), /* */ comments (line 72), src/href/data-* URL attribute values (48-49), url() bodies (51), font-family declarations (53), and // line-comments (57). There is NO <script>...</script> (or JS/JSON) body blanking — confirmed by grep over the whole file: the only "script" occurrence is in the argparse description line 213; no fs-deck-notes special-case exists. So any CJK inside a <script> on a line falls through to IDEO.search and is appended to `suspects` → printed as "untranslated Chinese (HARD)" and cmd_residual returns 1 (line 110).

2) render-deck.py:2902-2908 emits speaker notes verbatim into <script type="application/json" id="fs-deck-notes"> via json.dumps(..., ensure_ascii=False) (single line, default separators), so Chinese notes appear literally in the rendered index.html.

3) extract-text-pairs.py RunExtractor (lines 42-112) explicitly skips script bodies: handle_starttag increments _skip_depth for tag=="script" (77-78) and handle_data/entityref/charref early-return while _skip_depth>0 (98-112). Its docstring (43-44) says it collects CJK "not in script/style". So the translate (extract/apply) pipeline intentionally never extracts or swaps script CJK — confirming residual-cjk disagrees with the pipeline's own non-translatable decision (the contract-drift core of the claim).

4) translation.md confirms reachability: Branch-A re-renders to index.html via render-deck.py (line 80, which produces the fs-deck-notes island), and the QA section (lines 115-117) instructs running `residual-cjk` on the deck (index.html) as a HARD gate.

5) Reproduced directly: ran translation-qa.py residual-cjk on a minimal index.html whose only remaining CJK is inside <script id="fs-deck-notes"> — output reported "1 line(s) with untranslated Chinese (HARD)" pointing at the notes <script> and EXIT=1. So a fully-translated deck with Chinese speaker notes hard-fails the gate with no actionable fix.

Severity medium (not higher): it is a genuine unactionable HARD false-positive that blocks/confuses the QA gate and contradicts extract-text-pairs' script-skip contract, but it is conditional (only when the deck has non-empty Chinese slide.notes AND residual-cjk is run on the rendered index.html) and causes no data corruption or broken deliverable. The proposed fix (blank <script>...</script> bodies like comments are blanked, preserving newlines for line numbers, or special-case id=fs-deck-notes; optionally surface visible-script CJK as a soft note) is correct and consistent with the file's existing comment-blanking approach.

### `misc-4` — apply-text-pairs writes deck.json non-atomically and skips schema/HTML re-validation + rollback (bypasses the single-writer contract)
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `deck-json/apply-text-pairs.py:196-209`
- **Problem:** apply-text-pairs is a deck.json writer but does a plain (non-atomic) args.deck.write_text(...) and never re-validates the result. deck-cli.py is the documented single writer and its write_deck_with_validation does atomic_write_text (F-269: a kill mid-write must not leave a torn deck.json) PLUS a re-validate against the schema with rollback-on-fail. apply-text-pairs only keeps a timestamped .bak (good for manual recovery) but: (a) a process kill during write_text can leave a truncated/torn deck.json on disk with no auto-rollback; (b) a replacement that breaks the embedded data.html (e.g. a replace string containing an unbalanced tag or a stray quote inside an attribute) is written and never caught — the render/HTML gate that would catch it is never invoked here. atomic_write_text already exists in deck-cli.py:77 (and a fallback in render-deck.py:124) and could be reused.
- **Evidence:**
  ```
  bak.write_text(args.deck.read_text(encoding="utf-8"), encoding="utf-8")
  args.deck.write_text(
      json.dumps(deck, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
  print(f"  ✓ 写回 {args.deck.name} (备份 {bak.name})")
  return 5 if miss else 0
  # no atomic write, no schema re-validate, no rollback
  ```
- **Fix:** Write via atomic_write_text (temp file + os.replace) and, after writing, run validate-deck.py --strict (as deck-cli does); on failure restore from the .bak and exit non-zero. Reusing deck-cli's write_deck_with_validation (or its atomic_write_text) would also give the consistent optimistic-lock + rollback behavior the rest of the pipeline guarantees.

### `misc-5` — deck-log version numbering collides on v001 forever when Playwright is absent
- **Severity:** MEDIUM  ·  **Category:** robustness  ·  **Verdict:** unverified  ·  **Confidence:** high
- **Location:** `log-tool/deck-log.py:151-165, 619-652`
- **Problem:** _next_version()/_latest_version() derive the version number ONLY from screenshots/vNNN directories. When Playwright is not installed, _shoot() returns None and returns BEFORE creating the out_png_dir (line 506 return None precedes line 507 mkdir), so no screenshots/vNNN dir is ever created. But cmd_snapshot still appends a 'version' journal event unconditionally (with empty slides). Consequently every snapshot computes _next_version() -> 'v001' again, and the journal accumulates multiple distinct 'version' events all tagged v001. Downstream (_render_makingof._version_card, diff's by_v dict keyed on e.get('v'), _prev_slide_fingerprints) all assume v is a unique version id, so they collide/confuse. The auto-snapshot path (render-deck) and headless CI are exactly where Playwright may be missing, making this realistic.
- **Evidence:**
  ```
  def _version_nums(log_dir):  # only reads screenshots/vNNN dirs
      shots = log_dir / "screenshots" ...
  # _shoot returns None before any dir is made when playwright missing:
      except Exception:
          print("  ⚠️ 未装 playwright..."); return None
      out_png_dir.mkdir(...)  # never reached
  # cmd_snapshot still writes a version event regardless:
  append_event(log_dir, {"t": "version", "v": v, ... "slides": [...]})
  ```
- **Fix:** Derive the next version number from the journal's existing 'version' events (max v) rather than (or in addition to) screenshot dirs, OR skip appending the version event when _shoot() returns None (no screenshots captured) so numbering and journal stay consistent. Creating the version dir even on the no-shots path would also keep _version_nums monotonic.

### `contract-1` — validate-deck.py rule codes (R-CANVAS, R-FAMILY-DRIFT, R-DEMO-IFRAME) bypass the entire drift apparatus — undocumented in FAMILIES/yaml/md
- **Severity:** MEDIUM  ·  **Category:** contract-drift  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `deck-json/validate-deck.py:287-304, 377-389, 409-460`
- **Problem:** deck-json/validate-deck.py is the live deck.json-side schema+business validator (invoked by deck-cli lint, render-deck.py during render, conform-to-deck.py, and repair-lifted.py). It emits at least 7 distinct rule codes, three of which are UNIQUE to it and emitted nowhere else: R-CANVAS (canvas-slide element geometry/id/src checks), R-FAMILY-DRIFT (F-300 family-conformance advisory, which per F-320 even feeds the deck-cli pre-write lint), and R-DEMO-IFRAME (iframe-embed demo lost). The anti-drift machinery is structurally blind to this validator: enumerate_validate_rules() in check-only.py scans ONLY audits.js + run-audits.py + validate.py (its docstring explicitly says codes come from 'exactly three places'), and neither check-only.py nor check-rule-coverage.py references validate-deck.py. Result: R-CANVAS and R-FAMILY-DRIFT appear in NO registry surface at all (not in FAMILIES, not in business-rules.yaml, not in references/validator-rules.md), and R-DEMO-IFRAME appears only as prose buried in the R-IFRAME-REMOTE row of validator-rules.md (no first-class row, not in FAMILIES, not in yaml). All drift guards (check-rule-coverage.py exit 0; the 16 F-03/F-18 tests) stay green because they never look at validate-deck.py. New deck.json-side rules can therefore ship undocumented and ungated indefinitely — the same 'registered in some places not others' bug class the apparatus was built to prevent, just one validator beyond its reach. Verified: `grep -c R-CANVAS|R-FAMILY-DRIFT|R-DEMO-IFRAME assets/audits.js` = 0; absent from business-rules.yaml; R-CANVAS/R-FAMILY-DRIFT absent from validator-rules.md; check-rule-coverage.py prints '三方都是 85 条, 无漂移'.
- **Evidence:**
  ```
  # check-only.py enumerate_validate_rules() docstring:
  #   Codes come from exactly three places, scanned here:
  #     · assets/audits.js ...
  #     · assets/run-audits.py ...
  #     · assets/validate.py ...
  # (validate-deck.py NOT scanned)
  
  # validate-deck.py emits codes as free-text only:
  result.err(f"{sp}.data.html", "... (R-DEMO-IFRAME)")
  result.err(f"{sp}.elements[{e}].id", f"duplicate element id {eid!r} ... (R-CANVAS)")
  result.warn_soft(sp, ("D1 page-background: ... (R-FAMILY-DRIFT)" ...))
  ```
- **Fix:** Add validate-deck.py to the authoritative scan in check-only.py's enumerate_validate_rules() (a 4th source block). Since validate-deck.py emits codes only as free-text '(CODE)' inside messages, either (a) scan for the parenthetical convention `re.findall(r'\(([A-Z][A-Z0-9-]+|R\d+)\)', vd_src)` — fragile — or, better, (b) refactor validate-deck.py's Result.err/warn/warn_soft to take an explicit `rule=` code (structured, like audits.js findings) and scan that literal. Then register R-CANVAS, R-FAMILY-DRIFT, R-DEMO-IFRAME in FAMILIES (check-only.py), business-rules.yaml, and as first-class rows in references/validator-rules.md so check-rule-coverage.py and the F-03 tests cover them.
- **Verifier:** Independently verified every load-bearing claim against the actual code.

(1) The three codes ARE emitted by deck-json/validate-deck.py and nowhere else: R-CANVAS (lines 287,295,301,304), R-FAMILY-DRIFT (lines 398,448,454,460), R-DEMO-IFRAME (line 389). grep -c for all three in assets/audits.js and assets/run-audits.py = 0.

(2) validate-deck.py is a live production validator, not dead code: invoked by deck-json/deck-cli.py (lint + F-320 scope-aware pre-write lint, line 216 calls it with --strict --json), render-deck.py (line 80 VALIDATE_DECK), conform-to-deck.py (line 682, post-conform --strict gate with rollback), and repair-lifted.py (line 84). Confirmed by grep of call sites.

(3) The anti-drift apparatus is structurally blind to it. check-only.py enumerate_validate_rules() (lines 169-219) scans EXACTLY three files — audits.js, run-audits.py, validate.py — and the docstring says so explicitly ('Codes come from exactly three places'). validate-deck.py is never read. check-rule-coverage.py (line 33) defines 'engine全集' as that same enumerate set, so the FAMILIES/yaml diff guards cannot see validate-deck.py codes. Neither check-only.py nor check-rule-coverage.py references validate-deck.py (grep = 0).

(4) Cross-surface absence confirmed by grep: R-CANVAS = 0 in audits.js/yaml/check-only.py/validator-rules.md/run-audits.py; R-FAMILY-DRIFT = 0 in all; R-DEMO-IFRAME = 0 everywhere except 1 hit in validator-rules.md — which I read (line 26) and confirmed is prose buried in the R-IFRAME-REMOTE row, not a first-class row, exactly as claimed.

(5) The F-03 contract tests pass because they share the same blind enumerate set: test_all_emitted_codes_documented_in_families (test_check_only_gate.py:110) and test_validator_rules_reference_documents_every_code (line 122) both diff against CO.enumerate_validate_rules(), which cannot contain validate-deck.py codes. The docstrings say 'every rule code validate.py can emit' — they mean the unified engine adapter validate.py, NOT the similarly-named schema validator validate-deck.py; that name collision is the likely root of the blind spot.

(6) Running python3 assets/check-rule-coverage.py prints '✅ 规则覆盖对齐 —— 引擎 / FAMILIES / yaml 三方都是 85 条, 无漂移' — green, exactly as claimed, while three live rule codes sit ungated.

(7) The F-320 operational significance is real: TICKETS.md F-320 documents R-FAMILY-DRIFT flowing into deck-cli's scope-aware pre-write lint; validate-deck.py --json (lines 544-560) packs {path,msg,slide,key} but drops the rule code (codes are free-text inside msg), corroborating the suggested fix's premise.

Conclusion: this is a genuine, reachable contract-drift / SSOT-coverage blind spot — a whole live validator's rule codes (R-CANVAS, R-FAMILY-DRIFT, R-DEMO-IFRAME) escape the FAMILIES list, business-rules.yaml, validator-rules.md first-class rows, and every F-03/F-18 test + the check-rule-coverage gate, the exact 'registered in some places not others' class the apparatus exists to prevent.

Severity corrected from high to medium: it is a confirmed, real defect in a subsystem the codebase explicitly invests in, but the impact is governance/maintenance only — the validators still run and surface their codes, so there is no wrong output, no deck.json corruption, no silent data loss, and no crash. The harm is a false 'no drift' assurance plus two rules (R-CANVAS, R-FAMILY-DRIFT) having no human reference row, with risk of future undocumented/ungated rules. That is a medium contract-drift bug, not high.

### `audits-js-1` — Registered rule `L1/L2/L4` is missing from RULE_META and escapes the coverage contract test (regex can't parse its id)
- **Severity:** MEDIUM  ·  **Category:** contract-drift  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/audits.js:2637 (rule); 1002-1083 (RULE_META); enforced by deck-json/tests/test_rule_contract.py:27`
- **Problem:** audits.js registers a live rule `id: 'L1/L2/L4'` (line 2637) that emits findings with codes L1/L2/L4. The RULE_META coverage registry (lines 1002-1083) has NO entry for 'L1/L2/L4' (nor for L1/L2/L4 individually). The contract is supposed to be machine-enforced by test_rule_contract.py, but its id-extraction regex `\bid:\s*'([A-Z0-9][\w-]+)'` cannot match `'L1/L2/L4'` because `/` is not in `[\w-]` — so the rule is silently skipped by `test_every_rule_is_declared_in_rule_meta` and never flagged as undeclared. I reproduced this: the test parser returns 71 ids and reports MISSING=[] even though L1/L2/L4 has no meta. Separately, the in-engine `assertRuleContract()` (line 1087) that would also catch this is DEAD CODE — never invoked anywhere in audits.js. Net: a rule that the codebase's stated invariant requires to declare coverage has no declaration, and two independent guards both fail to notice. This is exactly the 'rule registered in some places not others' drift class the architecture flags as the most recurring bug.
- **Evidence:**
  ```
  id: 'L1/L2/L4',  // audits.js:2637 — no 'L1/L2/L4' key exists in RULE_META (1002-1083)
  // test regex (test_rule_contract.py:27): re.findall(r"\bid:\s*'([A-Z0-9][\w-]+)'", rules_region) → does NOT capture 'L1/L2/L4' (breaks at '/')
  // assertRuleContract defined at audits.js:1087 but grep shows zero call sites
  ```
- **Fix:** Either (a) split the rule into three rules with ids 'L1','L2','L4' (each emitting its own code) and add RULE_META entries, or (b) keep the composite rule but add a RULE_META['L1/L2/L4'] entry AND broaden the test regex to `'([A-Z0-9][\w/-]+)'` so slashed composite ids are parsed. Also actually CALL assertRuleContract(RULES.map(r=>r.id), RULE_META) at engine load (e.g. emit its violations as warn_soft findings) so the engine self-checks instead of relying solely on the text-parsing test. Confirm parity with check-only.py FAMILIES / business-rules.yaml (which already key on emitted L1/L2/L4 codes and are fine).
- **Verifier:** All four sub-claims independently confirmed by reading the code and running the test parser. (1) The live rule `id: 'L1/L2/L4'` exists at audits.js:2637 and emits codes L1/L2/L4 (lines 2651/2662/2674). (2) RULE_META (lines 1002-1083) has NO key for 'L1/L2/L4' nor for L1/L2/L4 individually — verified by parsing the meta block (all four membership checks returned False). (3) The contract test regex at test_rule_contract.py:27, `\bid:\s*'([A-Z0-9][\w-]+)'`, returns None on the line `id: 'L1/L2/L4',` because '/' is not in [\w-]; the parser yields exactly 71 ids with MISSING=[], and `pytest test_rule_contract.py` passes 4/4 — so the rule silently escapes `test_every_rule_is_declared_in_rule_meta`. (4) `assertRuleContract` (defined audits.js:1087) is dead code: the only two occurrences in the whole tree are the comment at line 998 and the definition itself; the IIFE only returns `rules: RULES.map(r=>r.id)` and never calls it. I confirmed that if it WERE called it would correctly flag 'L1/L2/L4: no RULE_META entry' (the runtime id list includes the composite id), and that broadening the regex to include '/' makes it match. So both guards genuinely fail and the stated invariant ('every rule in RULES must be declared in RULE_META') is violated. I downgrade severity from high to medium because the impact is confined to the coverage-declaration contract, not runtime validation: the EMITTED codes L1/L2/L4 are properly registered in the other surfaces (check-only.py FAMILIES lines 82-83, business-rules.yaml lines 70/79/216, validator-rules.md lines 36-37/151), so the rule still fires and its findings are categorized correctly — there is no wrong output or data-integrity consequence. It is a real, reachable contract-drift defect (the exact 'registered in some places not others' class) with latent risk for future slash-composite ids, but no current user-visible mis-validation, which makes it medium rather than high. Suggested fix is sound: add a RULE_META['L1/L2/L4'] entry and broaden the test regex to [A-Z0-9][\w/-]+, and actually invoke assertRuleContract at load (emitting violations as soft findings) so the engine self-checks.

### `audits-js-2` — No-browser path ignores `scope` for per-slide source-text rules (R02/R07/R-ESC-HTML), reporting off-scope slides
- **Severity:** MEDIUM  ·  **Category:** correctness  ·  **Verdict:** confirmed  ·  **Confidence:** high
- **Location:** `assets/run-audits.py:1037-1057 (no-dom branch); 749-762 (runner_no_browser_text_findings); 635-665 / 609-632 (audit_structure_bytes / audit_escaped_html_bytes)`
- **Problem:** run_unified_engine(html_path, scope, ..., dom_rules=False) accepts a `scope` argument and echoes it in the result, but on the no-browser branch it calls runner_no_browser_text_findings(raw_html) which scans EVERY slide-frame and emits findings for all of them, never filtering by scope. The per-slide rules among these — R02 (missing data-layout/screen-label), R07 (missing .wordmark), R-ESC-HTML (escaped markup) — therefore report off-scope slides. On the VISUAL path (dom_rules=True) the audits.js driver (audits.js:6853-6855) skips off-scope slides entirely for the very same rule codes. So `validate.py --no-visual --scope-frames N` and `validate.py --visual --scope-frames N` give divergent findings for the same deck: the static/no-Chromium path leaks a pre-existing R-ESC-HTML/R02 error on an unrelated slide and can block a scoped edit. I reproduced it: a 2-slide deck with an escaped tag only on slide 2, run with scope=[1] dom_rules=False, still emits the slide-2 R-ESC-HTML finding. (render-deck.py's F-319 wrapper papers this over by re-running --no-visual --json and re-parsing the slide number from the message text — brittle and not applied when validate.py is invoked directly.)
- **Evidence:**
  ```
  if not dom_rules:
      text_findings = runner_no_browser_text_findings(raw_html)   # run-audits.py:1043 — `scope` is in signature but NOT passed/applied
      all_findings = runner_findings + text_findings
      ...
      "scope": list(scope) if scope else None,   # echoed but never used to filter
  ```
- **Fix:** Pass `scope` into runner_no_browser_text_findings and have the per-slide rules (audit_structure_bytes / audit_escaped_html_bytes) skip frames whose 1-based index is not in scope (mirroring audits.js driver). Genuinely deck-level byte rules (R05 audit_copy_bytes, R-KEY duplicate scan) stay scope-independent like their audits.js deck-level twins. Alternatively, document explicitly that the no-visual path is whole-deck-only and reject `--scope-frames` with `--no-visual`. Either way, make the two engine paths agree so scoped validation is consistent.
- **Verifier:** Independently confirmed and reproduced. run-audits.py:1037-1057 — the dom_rules=False branch calls runner_no_browser_text_findings(raw_html) with NO scope argument; `scope` is only echoed at line 1053 ("scope": list(scope) if scope else None) and never used to filter. runner_no_browser_text_findings (749-762) and the per-slide byte rules audit_structure_bytes (R02/R07, 635-665) and audit_escaped_html_bytes (R-ESC-HTML, 609-632) iterate every _extract_slide_frames frame with 1-based index and emit per-frame, no scope check. On the visual path the audits.js driver (audits.js:6855: `if (scopeSet && !scopeSet.has(slide_idx)) return;`) skips off-scope slides, and R-ESC-HTML (1290), R02-R07-STRUCTURE (2968), R07 (1847) are pure per-slide evaluate(slide,ctx) rules, so they ARE scope-filtered there. The claim's per-rule classification is exactly right: R05 (audits.js:2330) and R-KEY (1761) guard `if (!ctx.isFirstInScope) return []` and scan the whole deck (deck-level, scope-independent on both paths), and their byte twins audit_copy_bytes/audit_slide_keys_bytes are whole-deck too — so only R02/R07/R-ESC-HTML diverge. Reproduced at two levels: (1) engine — run_unified_engine(scope=[1], dom_rules=False) on a 2-slide deck with an escaped <span> only on slide 2 still emits the slide-2 R-ESC-HTML finding; (2) CLI — `validate.py --no-visual --scope-frames 1` and `--scope-frames 2` produce IDENTICAL output (both report slide-2 R-ESC-HTML), proving --scope-frames has zero effect on the no-visual per-slide rules. This directly contradicts the --scope-frames help text (validate.py:520-531: "this sets window.__AUDIT_SCOPE__ so the engine SKIPS evaluating off-scope slides entirely … per-slide audits only run on these frames"), so it is a documented-contract breach, not just path divergence — there is no mutual-exclusion guard rejecting --no-visual + --scope-frames. The F-319 wrapper (render-deck.py:3067-3116) re-runs `validate.py --no-visual --json` and re-attributes each finding via e.get("slide"), which validate.py:614/620 re-parses from the message text with _SLIDE_IN_MSG (since byte findings carry slide_idx:0) — confirming the brittle text-reparse the claim describes; this masks the leak only for render-deck.py's --scope flow, NOT for direct validate.py callers (e.g. the write-hook). Severity medium is honest: it yields wrong/divergent validator findings and can block a scoped edit when validate.py is called directly, but the dominant render-deck.py --scope flow is papered over by F-319 and the mandatory --final whole-deck gate still catches real leaks, so it is not high/critical. The suggested fix (thread scope into runner_no_browser_text_findings and skip off-scope frames in the per-slide byte rules while keeping R05/R-KEY whole-deck, or reject --no-visual + --scope-frames) is correct and mirrors the audits.js driver.


## LOW (49)

| id | title | location | cat |
|----|-------|----------|-----|
| `mutation-4` | L-TYPESCALE lint false-positive on CSS custom properties ending in 'font-size' wrongly REFUSES set-page | `deck-json/_lint_fragment.py:107-115` | correctness |
| `mutation-6` | import-html-slide.py insert path has no optimistic lock — concurrent edits silently clobbered | `deck-json/import-html-slide.py:611, 844, 859-861` | data-integrity |
| `mutation-7` | Dead regexes SLIDE_FRAME_RE and LOOSE_SLIDE_RE never used | `deck-json/import-html-slide.py:210-218` | reuse-duplication |
| `mutation-8` | parse_value silently type-coerces numeric/bool/null string values for `set` | `deck-json/deck-cli.py:103-112, 448` | robustness |
| `delivery-4` | package-deliverable.sh --name accepts ../ and writes the zip outside the output dir | `assets/package-deliverable.sh:27, 114-119` | security |
| `delivery-6` | magic-page-assets.py: is_probable_resource_attr is dead code, so <a href> navigation links are treated as upload resources | `assets/magic-page-assets.py:84-96, 325-332` | correctness |
| `delivery-7` | magic-page-assets.py download_external_ref reads remote bodies into memory with no size cap | `assets/magic-page-assets.py:244-249` | robustness |
| `delivery-8` | shoot-page.py leaks the Chromium browser process if screenshot/navigation throws (no finally close) | `assets/shoot-page.py:82-98` | robustness |
| `prose-2` | editing-discipline.md `deck-cli insert` example mis-positions `'{}'` as the VARIANT argument | `references/editing-discipline.md:253-254` | contract-drift |
| `prose-3` | Simulator's documented entry-gate artifact `audit-report.json` is produced by no documented validator step (dangling contract) | `subskills/simulator/SKILL.md:71, 75-78` | contract-drift |
| `prose-5` | Root SKILL.md References list omits `references/extra-layouts-and-raw.md` (renderer loads it) | `SKILL.md:369-397` | contract-drift |
| `prose-6` | Simulator uses logical role aliases (`deck-validator` / `deck-designer` / `deck-renderer` / `pitch-simulator`) that don't match the actual subskill paths used everywhere else | `subskills/simulator/SKILL.md:27-29, 33-45, 171-179` | style |
| `frontend-4` | First-paint reveal suppression misses the first VISIBLE slide when slide 0 is hidden (initial paint animates ~700ms) | `assets/feishu-deck.js:935` | correctness |
| `frontend-3` | Re-running init() leaks the old .deck-ui overlay (duplicate, partly-dead control bar) | `assets/feishu-deck.js:436-451` | robustness |
| `frontend-5` | enterEditMode() guards deck for some operations but would throw on null deck for others | `assets/edit-mode/deck-edit-mode.js:83-143` | robustness |
| `lift-1` | reconcile-lifted rewrites font px inside CSS comments in a rule body (mutates author annotations, inflates change counts) | `deck-json/reconcile-lifted.py:259-260` | correctness |
| `lift-2` | repair-lifted docstring falsely claims steps 3-5 're-validate with rollback'; heal/clean/reconcile do neither | `deck-json/repair-lifted.py:46-47` | contract-drift |
| `lift-3` | lift-slides step 6b uses lstrip('./') anti-pattern, mangling ../ paths and defeating its own ../-skip guard | `assets/lift-slides.py:970-971` | correctness |
| `lift-4` | extract_head_slide_rules re-anchors OTHER pages' selectors in a multi-target comma rule onto THIS slide's key | `assets/lift-slides.py:586-592` | correctness |
| `lift-6` | cmd_scan inline-<script> report counts the framework script despite claiming 'outside the framework' (moot if F-307 stays reverted) | `assets/lift-slides.py:(F-307 cmd_scan block — currently reverted out of the working tree)` | correctness |
| `lift-5` | lift() new-file path has no concurrent-create guard (optimistic lock only checks pre-existing files) | `assets/lift-slides.py:1049-1053, 1244-1245` | robustness |
| `diff-3` | F-319 runs validate.py twice on a scoped gate failure (redundant subprocess / cold-start cost) | `deck-json/render-deck.py:3063, 3084-3087` | efficiency |
| `diff-4` | F-319 in-scope-block branch leaves GATE-COVERAGE static= label as plain "ran" (mislabels a scope-demoted-but-blocked run) | `deck-json/render-deck.py:3095, 3107-3114` | style |
| `renderer-4` | _esc_br injects a literal <br> into HTML attribute values when user text contains a newline | `deck-json/render-deck.py:196-209` | correctness |
| `renderer-5` | iframe-embed src has no scheme allow-list (javascript:/data: pass through attribute-escaping unchanged) | `deck-json/render-deck.py:1674-1723` | security |
| `renderer-6` | _canonicalize asset-prefix collapse can mask a real edit to an authored path containing 'assets/' | `deck-json/_index_sig.py:53-54, 70-73` | robustness |
| `renderer-7` | Unused `total` parameter in render_slide (dead signature carryover) | `deck-json/render-deck.py:2018` | reuse-duplication |
| `reskin-2` | merge_slide drops non-first runs of a multi-run host element (silent styling loss) | `deck-json/merge-canvas-lines.py:108-113` | data-integrity |
| `reskin-3` | Band running-mean lets a y-gradient of fragments merge two separate visual lines | `deck-json/merge-canvas-lines.py:82-89` | correctness |
| `reskin-7` | strip_drop_shadows offset parser misses fractional offsets without leading zero and non-px units → drop shadows kept | `assets/reskin.py:411-419` | correctness |
| `reskin-8` | scale_canvas skips any whole line containing 'font-size' → minified CSS gets no scaling at all | `assets/reskin.py:469-474` | correctness |
| `reskin-9` | _frame_spans naive <div>/</div> counting miscounts depth on </div> inside <script>/<style>/comments | `deck-json/migrate-head-css-to-custom-css.py:82-92` | robustness |
| `reskin-10` | migrate_raw_inline whole-slide idempotency gate strands a <style> re-added after a prior migration | `deck-json/migrate-head-css-to-custom-css.py:300-302` | robustness |
| `reskin-11` | merge-canvas-lines uses an mtime guard, not the deck-cli index-signature lock, and never re-renders/validates | `deck-json/merge-canvas-lines.py:186-201` | robustness |
| `subskill-7` | PDF text extraction silently drops octal-escaped characters, corrupting recovered text | `subskills/parser/parse.py:343-363` | correctness |
| `subskill-9` | data-* attributes are misdetected as src/href dependencies by RESOURCE_ATTR_RE (\b boundary inside hyphenated attr names) | `subskills/publisher/publish.py:41,250-257` | correctness |
| `subskill-8` | Publisher report template references payload keys (mode/publish_dir) that are never populated | `subskills/publisher/publish.py:405-423` | reuse-duplication |
| `sync-2` | Canvas integer geometry becomes float on sync → phantom drift trips F-315 clobber guard (exit 11) on clean decks | `deck-json/sync-index-to-deck.py:287-295 (_canvas_geom_from_style), 1076-1085 (canvas drift compare + lossy_drift)` | correctness |
| `sync-6` | conform-to-deck --apply re-serializes whole slide HTML via BeautifulSoup, rewriting unrelated void-tag formatting | `deck-json/conform-to-deck.py:482-500 (fix_pretitle_chrome)` | robustness |
| `misc-2` | apply-text-pairs HTML branch chained-replacement: a substituted string can be re-matched by a later find (double-translation) | `deck-json/apply-text-pairs.py:143-153` | data-integrity |
| `misc-6` | extract-text-pairs HTML run ordering is non-deterministic across processes (redundant set() destroys insertion order for equal-length runs) | `deck-json/extract-text-pairs.py:138` | correctness |
| `misc-9` | deck-log: snapshot version event is written even when no screenshots are produced, decoupling journal versions from disk | `log-tool/deck-log.py:643-654` | correctness |
| `misc-7` | deck-map div depth-matcher miscounts around stray </div>/<div in HTML comments (and script/style), truncating frames and dropping titles | `deck-json/deck-map.py:30-59` | robustness |
| `misc-8` | translation-qa parity reports BRANCH A (PASS) for an empty/frameless roundtrip render | `deck-json/translation-qa.py:122-158` | robustness |
| `contract-2` | validator-rules.md has no reverse-direction guard (documented-but-never-emitted rules go undetected) | `deck-json/tests/test_check_only_gate.py:122-134` | contract-drift |
| `contract-3` | enumerate_validate_rules() audits.js scan misses any rule emitted via a non-literal `rule:` (latent, not biting today) | `assets/check-only.py:194` | robustness |
| `audits-js-3` | R-LANG user-facing messages point to the wrong file for the brand whitelist (`LATIN_BRAND_WHITELIST in validate.py`) | `assets/audits.js:1986, 2039 (messages); 301 (actual definition)` | contract-drift |
| `audits-js-4` | parse_scope accepts 0 / negative / reversed ranges silently, turning a scoped run into a no-op false PASS | `assets/run-audits.py:27-41 (parse_scope); 6841-6855 (audits.js driver uses the set)` | robustness |
| `audits-js-5` | Per-rule cap collapse marker is emitted as `warn` even for error-class rules and is exempt from the cap itself | `assets/audits.js:6888-6912 (PER_RULE_CAP collapse)` | robustness |

## Refuted by adversarial verification

- `prose-4` (docs-and-routing-consistency): Simulator points at `schema/pitch-rehearsal.schema.json`, but the file lives under `subskills/simulator/schema/` — Verified the raw facts: subskills/simulator/SKILL.md:85 reads "pitch-rehearsal.json   # 结构化结果,符合 schema/pitch-rehearsal.schema.json"; the top-level schema/ dir (ingestion-manifest, outline, source-dossier, validate-contract.py) does NOT contain pitch-rehearsal.schema.json; the schema lives at subskills/simulator/schema/pitch-rehearsal.schema.json. So the surface facts are correct.

However the claimed DEFECT does not hold up. The claim manufactures the bug by forcing the importer's path convention onto the simulator doc. But the importer references its manifest schema with a FULLY-QUALIFIED path (ingest.py:650 and importer/SKILL.md:151 both use "skills/feishu-deck-h5/schema/ingestion-manifest.schema.json"), not a bare "schema/..." — so there is no shared bare-path-is-root-relative convention to drift from. The simulator SKILL.md itself uses fully-qualified paths for its actual commands (L94 validate-rehearsal.py and L101 simulate-pitch.py both prefixed with skills/feishu-deck-h5/subskills/simulator/). Line 85's bare "schema/..." is the lone exception and is a prose comment label inside a code block, never consumed by any tool.

I tested both relative interpretations: from the simulator dir, ./schema/pitch-rehearsal.schema.json EXISTS (subskill-relative reading resolves correctly); from the skill root it does not. Under the natural reading a simulator-subskill maintainer would use, the documented path is correct, not broken.

Runtime impact is nil and the claim concedes this: grep over subskills/simulator/*.py shows validate-rehearsal.py:60 only reads self.path (the input JSON), simulate-pitch.py:48 only reads input artifacts; neither imports jsonschema nor opens any .schema.json. validate-contract.py takes an explicit --schema arg and never resolves the doc string.

Net: the strong assertion ("resolves to a nonexistent file") is only true under a convention the codebase does not actually use; under the natural subskill-relative reading the path resolves to an existing file, and nothing breaks at runtime. At most this is a cosmetic prose ambiguity (bare path vs fully-qualified paths elsewhere in the same file), not a real contract-drift defect. Refuted; severity none.

## Per-subsystem health

### renderer-core (7 findings)
The renderer is mature and unusually well-defended: atomic writes, a pre-render clobber guard with auto-sync, gate-fail rollback of the previous index.html, careful HTML-escaping of user text through one _esc_br path, scoped per-slide custom_css, and an idempotent provenance/self-sig scheme. Most "raw HTML/SVG injection" surfaces (raw layout, canvas svg, icon {svg}, root.why, raw-svg visual) are deliberate author-trusted escape hatches consistent with the deck.json-as-SSOT model, not security bugs. I found two genuine correctness/data-integrity defects: (1) the single-file --inline path inlines the WRONG image source when an <img> carries data-src/data-* before src, leaving the real displayed image external so the "portable" deck 404s once moved, and it slips past --inline-strict; (2) render --renumber's leading-number-stripping regex over-matches alphanumeric title tokens (5G/3D/24小时…), silently deleting them from screen_label and PERSISTING the loss back to deck.json. A lower-severity silent data-loss exists in flow/swim when two milestones share a quarter (last wins, no warning). Everything else (offset math, scope/iter/baseline bookkeeping, custom_css scoping, sig idempotency, grow-box font regex) checked out under direct testing.

### validator-engine-js (5 findings)
The unified JS validator engine (audits.js) and its Python runner (run-audits.py) are mature, carefully written, and heavily defended — geometry math correctly normalizes to design px via /scale, try/finally cleanly restores forced is-current state, the per-rule cap is non-silent, byte rules avoid double-emit, and most rules carry name-free coverage with documented opt-outs. The most significant defect class is the one the architecture warns about: validator-rule registration drift. One real registered rule (`id: 'L1/L2/L4'`) is entirely absent from RULE_META, and the contract test designed to catch exactly this cannot parse the rule's id (its regex breaks on the `/`), so the gap is silently uncaught; the in-engine `assertRuleContract()` that would catch it is dead code (never called). The second real issue is a scope-filtering asymmetry: `run_unified_engine`'s no-browser path ignores the `scope` argument for the per-slide source-text rules (R02/R07/R-ESC-HTML), reporting off-scope slides, whereas the visual path filters them — divergent results for the same deck under `--scope-frames` (partially papered over downstream by render-deck's brittle message-regex re-attribution). Remaining findings are lower severity (stale user-facing file references, unvalidated scope ints). No data-corruption or arbitrary-write defects were found in these two files.

### validator-contract-drift (3 findings)
The forward-direction drift apparatus is healthy: `enumerate_validate_rules()` (check-only.py) authoritatively scans audits.js + run-audits.py + validate.py, and all three downstream surfaces are in lockstep — `check-rule-coverage.py` reports engine/FAMILIES/yaml all at 85 codes with zero drift, and the 16 F-03/F-18 contract tests in test_check_only_gate.py pass. I verified run-audits.py emit idioms are fully captured (no gap) and that _validate_common.py / check-distribution.py legitimately live outside the registry (helpers + a separate geometric-signal namespace). HOWEVER there is a structural blind spot: the apparatus's own design (and the check-only.py docstring) treat the rule source as "exactly three places" and completely exclude deck-json/validate-deck.py, which is the live deck.json-side schema+business validator wired into deck-cli lint, render-deck, conform, and repair. validate-deck.py emits 7 rule codes embedded as free-text "(CODE)" in messages; 3 of them (R-CANVAS, R-FAMILY-DRIFT, R-DEMO-IFRAME) exist in NO registry surface — they are undocumented in FAMILIES, business-rules.yaml, and (for two of them) validator-rules.md, and every drift guard stays green because it never looks at validate-deck.py. This is exactly the "rule registered in some places not others" recurring bug class, just one validator further out than the guards reach. Secondary: validator-rules.md is the weakest-guarded surface (only engine⊆md is tested; no reverse md⊆engine check exists in either check-rule-coverage.py or any test), so a retired/typo'd rule code can linger in the human reference as if live.

### deck-mutation-cli (8 findings)
deck-cli.py is the well-hardened single writer: atomic writes, optimistic lock, in-memory + .bak rollback, scope-aware demotion, and the F-315 clobber guard are all coherent and correct on the paths I traced. The real risk concentrates in the SECOND, parallel writer — import-html-slide.py — which mutates deck.json WITHOUT adopting any of deck-cli's protections: it uses a non-atomic write_text, runs NO deck-schema validation before writing, and never rolls back its .bak when the post-insert re-render fails, so a failed import leaves an invalid/torn deck.json on disk with only a manual backup. That is the highest-stakes defect in this partition (data-integrity divergence from the SSOT contract). Two genuine path-traversal/arbitrary-file-write surfaces exist in the asset-copy code of both paste (deck-cli) and import (import-html-slide), where the input/, prototypes/, assets/shared/ scan loops let a "../" capture escape the destination deck dir when copying from a foreign/crafted source deck — notably the author already added the containment guard to import's inner recursion but not its top-level loop, and the bare-media loop in deck-cli guards "../" but the structured loops do not. The pre-write lint (_lint_fragment) has a false-positive that can wrongly REFUSE a legitimate set-page write (a CSS custom property whose name ends in "font-size"). Minor: dead regexes in import, parse_value silent type-coercion footgun (self-corrected by validation).

### sync-roundtrip (6 findings)
The raw/custom_css/order/hidden/notes reverse-sync paths are well-engineered: scope_selectors is genuinely idempotent so custom_css drift is sound, the direction guard / baked-DOM guard / F-315 check-drift exit-code contract are coherent, and the surgical reconciles are lossless. The CANVAS reverse-map (sync_canvas_data / _runs_from_inner / _canvas_geom_from_style), however, has a serious silent-data-loss defect: it reconstructs runs from only text/bold/color, dropping the size/font/grad fields that render-deck emits and that pptx-to-deck (the dominant canvas producer) always sets — so a single browser text edit on a canvas slide, when synced, strips per-run typography from EVERY text element on EVERY canvas slide. The same path also turns integer geometry into floats and drops src/svg on newly-added elements, producing phantom drift that wrongly trips the F-315 clobber guard (exit 11 → deck-cli refuses) on cleanly-edited canvas decks. The backfill EXACT path loses content for duplicate data-slide-keys (renames the key but copies the first slide's inner HTML). The test fixture only exercises text/bold/color runs, which is exactly why these run-field losses are untested. conform/reflow are lower-risk; reflow writes deck.json without the post-write validate+rollback that conform has.

### lift-reconcile (6 findings)
The lift-reconcile subsystem is in good health and visibly battle-hardened: the F-40 anchor fusion ([data-page=N]→.slide[data-slide-key=K]), the F-76 shared asset-ref pattern set (verified used by both the scan/report and the copy steps — img/iframe/source/video/url all covered; no real deck uses srcset/svg-href/object so the set is complete in practice), the --shake key de-collision + _rekey_inner_css follow-through, the write-after-validate-with-rollback in lift(), and the comment-aware keyframe-prefix strip in clean-lifted-css are all correct and idempotent (I verified de-collision, rekey, F-40 fusion, snap_tier ties, clean-lifted-css idempotence, and step-4 path-rewrite idempotence with live snippets). The defects found are LOW severity: (1) reconcile-lifted rewrites font px that sits inside CSS comments in a rule body, mutating author annotations and inflating change counts (render-neutral, converges); (2) repair-lifted.py's docstring falsely claims steps 3-5 "re-validate with rollback" — none of heal/clean/reconcile re-validate or write atomically (they only back up via shutil.copy2), so the orchestrator's safety promise is overstated; (3) lift-slides step 6b uses the lstrip("./") anti-pattern that _strip_leading_dotslash was created to fix, defeating the intended ../-skip guard for "other" assets (latent — no observable wrong output, the mangled path simply fails the is_file check); (4) a few minor robustness/over-application edges. No corruption, no exploitable traversal, no silent-data-loss. NOTE: the uncommitted F-307 --scan code I was asked to review vanished from the working tree mid-review (a concurrent session reverted it — git diff/status now clean, file = HEAD@1818 lines), so --scan findings are moot; the rest of the file body is unchanged and all findings below are against the live on-disk version.

### reskin-migrate (11 findings)
The reskin/migrate subsystem is built almost entirely on regex- and substring-based CSS/HTML parsing rather than real parsers, which is the root cause of most defects found. The highest-impact issue is silent content loss in reskin's find_main_container (substring "slide" match selects a decorative/page-number div, dropping the whole slide body). merge-canvas-lines has two real data/geometry bugs: multi-run host elements lose their non-first runs' styling on merge, and the running-mean band algorithm lets a y-gradient of fragments "walk" two separate visual lines into one. scope_selectors fails to scope @supports blocks (CSS leaks across all slides), the very isolation the function exists to provide. rebundle-import's script-src regex misses any <script> with extra attributes or a cache-bust query, silently skipping its primary job. Lower-severity issues include unescaped title/subtitle interpolation, strip_drop_shadows missing fractional/unit offsets, scale_canvas skipping whole lines that mention font-size, naive div-counting in _frame_spans, and a whole-slide idempotency gate that strands styles re-added after a prior migration. None of these corrupt deck.json irrecoverably (backups are written), and several live only in the retired hybrid-pipeline path, but the content-loss and CSS-leak bugs are genuine correctness defects in the active reskin path.

### frontend-runtime (5 findings)
The frontend runtime is mature and carefully built: listeners are bound through a single AbortController, the F-315 stale-signature edit-clobber guard is sound (the edit-mode save deliberately re-serializes the DOM so the embedded fs-render-sig no longer matches), the Magic Move path is correctly triple-gated (opt-in attr + feature detection of startViewTransition + prefers-reduced-motion + never-on-first-paint), the kiosk hash regex correctly avoids false positives like #proj-2026, and the undo stack's mixed pre/post-mutation snapshot pattern happens to be self-consistent. I found four real defects. The most user-visible is a correctness bug in updateUI: the prev/next button enabled/disabled state compares a RAW frame index against a VISIBLE-slide count, so any deck with hidden (隐藏页) slides not at the tail mis-disables the on-screen nav controls (a deck with leading hidden slides shows Next disabled on the first real slide). A data-integrity gap: the edit-mode ⌘S save sanitizer strips only the canvas-center !important top/bottom but misses all the balanceColumns runtime !important geometry (flex/min-height/height/width/background-size/max-height/aspect-ratio + cell justify-content), so runtime-computed image-aspect geometry gets baked into index.html and folded back into deck.json on sync — the exact compounding the sanitizer was built to prevent. A robustness bug: re-running init() (an exposed API exercised by a test) aborts the old controller but never removes the old .deck-ui overlay, leaving a duplicate, partly-dead control bar. Plus a low-severity cosmetic reveal-suppression gap and a defensiveness inconsistency in enterEditMode.

### delivery-packaging (8 findings)
The delivery-packaging subsystem is mostly solid: copy-assets.py is idempotent, handles legacy redirects, path-traversal guards, and the prune/manifest logic round-trips cleanly; package-ingest.sh has strong symlink/metadata rejection and id validation; finalize.sh's run_step error capture is correct (set -e is disabled in the if-condition context). However I confirmed one HIGH-severity data-leak/bloat bug in the remote delivery path: copy-assets --shared=link creates output/assets/shared as a symlink to the full 30 MB skill pool, and package-deliverable.sh's cp -R preserves that symlink, then zip -r dereferences it — so a 'remote' deliverable for a customer who referenced ONE logo ships a 26 MB zip containing all 259 other customers' clientlogos plus .DS_Store. I also confirmed: (a) inline-assets.py img inlining uses str.replace on the whole tag, corrupting any earlier attribute that equals the src value; (b) the recent copy-assets missing-asset commit (6244ed7) warns but still lists missing files in assets-manifest.yaml, so the manifest stays inconsistent with package contents; (c) package-deliverable.sh --name allows ../ path-traversal writes outside the output dir; (d) shoot.py silently mislabels a screenshot when the nav guard can't reach a hidden/skipped target frame; (e) magic-page-assets.py's is_probable_resource_attr is dead code so <a href> nav links are treated as upload resources, and download_external_ref reads remote bodies with no size cap.

### subskill-tools (9 findings)
The subskill executables are generally well-structured, defensive (never-raise conversion paths, untrusted-source marking per F-287, deterministic simulator), and route real cloud writes through external scripts with PR-not-merge defaults. The highest-stakes problems concentrate in the PUBLISHER safety gate: it (1) only enforces the deck-validator pass when a deck.json happens to sit in the run output dir, so publishing an external --html to the live Magic Page service skips the gate entirely, and (2) never re-validates the actual bytes about to be published — it only reuses a stale render-time audit verdict, directly contradicting the publisher SKILL.md contract that mandates re-validation on the publish bytes (notably R-BAKED-DOM). The parser's HTML-asset materializer follows .. references out of the source directory and copies arbitrary readable files into the deliverable, a path-traversal read on untrusted input. The publisher post-publish self-check can pass vacuously (ok:true) when no slides pair up or all screenshots fail, so the visual/font dimensions are silently skipped. Lower-priority: no subprocess timeouts on network-bound bootstrap/publish calls, and the importer reuse path can apply a stale audit verdict to a different --html.

### translation-and-misc-tools (9 findings)
The translation and misc tools are generally careful, well-commented, and defensively coded (the transcript-scraping cross-timezone handling, the no-Playwright graceful-degrade paths, the canvas-vs-html dual matching, and the optimistic-lock/backup in apply-text-pairs are all thoughtfully done). The most consequential real defects are (1) deck-map.py's deck.json page index NOT skipping _disabled slides, so it disagrees with the canonical "page N = frame_index N" rule that locate-slide.py and the renderer honor — the tool whose entire purpose is the true page map can report a wrong page number; (2) a chained-replacement double-translation hazard in apply-text-pairs.py's HTML branch where an already-substituted string can be re-matched by a later find; and (3) translation-qa residual-cjk flagging CJK inside <script> bodies (notably the renderer's fs-deck-notes speaker-notes island and any inline JSON/JS) as HARD untranslated findings that the extract/apply pipeline never offers for translation — a coverage contract-drift that yields unactionable false-positive failures. Secondary issues: apply-text-pairs writes deck.json non-atomically and skips the schema/HTML re-validation+rollback that deck-cli (the documented single writer) performs; deck-log version numbering collides on "v001" forever when Playwright is absent; extract-text-pairs HTML run ordering is non-deterministic across processes; and deck-map's div depth-matcher miscounts/truncates around stray </div> in HTML comments, silently dropping titles. No critical corruption-without-recovery bug was found (apply-text-pairs does keep timestamped backups).

### live-diff-review (4 findings)
The actual uncommitted diff is healthy overall and smaller than the partition brief implied (no lift-slides.py / test_lift_slides.py / subskills/editor changes are present in the working tree — those are already committed or absent). Two real changes: F-322 in audits.js (two false-positive carve-outs for R-VIS-CARD-OVERFLOW) and F-319 in render-deck.py (scope-aware static gate that demotes out-of-scope pre-existing static errors instead of rolling back a scoped edit). F-322 is well-designed and well-tested (new test exercises all three branches and passes against live Chromium; no rule-id added so no contract drift; rule already in business-rules.yaml + validator-rules.md). F-319 is mostly sound and fails safe, and correctly leaves the full/`--final` whole-deck gate untouched — but it has one genuine correctness hole: it attributes static findings to slides by regex-parsing "slide N" out of the message text, and the per-slide content rule R05 (banned emoji / '!' / '…' / '???') emits body-wide messages with NO slide number, so an R05 error introduced by the in-scope edit is mis-classified as deck-level/off-scope and silently demoted (no rollback) on a scoped or default auto-scoped render. The mandatory `--final` gate would still catch it before delivery, so it's a gate-weakening bug, not a ship-to-customer corruption. Separately, the F-319 gate path (which controls index.html rollback) ships with ZERO regression tests for its demote-vs-block decision. No security, no deck.json-corruption, no contract-drift issues found.

### docs-and-routing-consistency (6 findings)
The controller/subskill instruction set is, on the whole, unusually well-aligned with the actual code: the Authoritative Mode Enum (request-router.md) and the SKILL.md Mandatory Router agree on all 12 modes; every named deck-cli/render-deck/sync/fast-text flag and exit code I spot-checked (paste, set-page, insert, --renumber/--iter/--scope/--quick/--final, clobber-guard render-exit-8 / deck-cli-exit-6, fast-text exit-3 + < > refusal, sync --check-drift exit 0/10/11, translation-qa parity/residual-cjk --strict-fullwidth/overflow, publish/ingest flags, conform D1-D5) exists and behaves as documented; and the cross-page rules importer claims to gate (R-DECK-TITLE/PALETTE/TYPESCALE) are registered in audits.js + business-rules.yaml + validator-rules.md. The real defects are a small cluster of doc-vs-code mismatches on the delivery/structural-edit hot paths: (1) delivery.md tells the agent to inline a customer deck via `build.sh --inline`, but that command only rebuilds the skill's bundled SAMPLE deck (examples/sample-deck-inline.html) — the per-run inline tool is actually inline-assets.py / render --inline; (2) editing-discipline.md's `deck-cli insert 7 raw '{}' <key>` example mis-positions `'{}'` as the VARIANT argument (the canonical signature is `insert POSITION LAYOUT [VARIANT] KEY` with no data payload); (3) the simulator's documented entry-gate artifact `audit-report.json` is produced by no documented validator step and is not consumed by its own simulate-pitch.py; (4) the simulator points at `schema/pitch-rehearsal.schema.json` which actually lives under subskills/simulator/schema/. None corrupt deck.json, but #1 and #2 can ship the wrong artifact / a malformed scaffold slide.
